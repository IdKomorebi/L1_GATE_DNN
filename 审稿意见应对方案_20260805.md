# 论文重构方案：从特征选择方法到推断风险审查

> 日期：2026-08-05（V2，立意重构版）
> 原稿：基于多相关性筛选与 L1 门控深度神经网络的电力数据隐性推断源定位方法
> 状态：两位审稿人均否定定位，需重构问题定义后大改重投

---

## 第一部分：论文该问什么问题

### 1.1 现在问错了

当前论文问的是：**给定目标字段 Y，找出能预测它的字段集合 S。**

这是特征选择。换成任何行业的任何高维回归数据集，全文一个字都不用改——这就是"方法通用、脱离电力场景"这句话的全部含义。

### 1.2 应该问什么

> **给定一批"各自合规"的已定级字段，判断它们的组合是否会突破保护边界；若会，判断这种突破是已知常识还是未被认识的隐性通道，并定位造成突破的最小字段组。**

转向的依据是一个观察：

现行电力数据分类分级是**逐字段、静态、定性**的，隐含假设为"字段的敏感度由其自身语义决定"。而真实的敏感度是：

```
字段真实敏感度 = max( 自身语义级别, 该字段参与推断出的最高级字段的级别 )
```

两者之差即**推断导致的定级失效**。逐字段审查全部通过、组合起来却泄露——现行规范对此完全失语。

**论文的定位由此确定：不给字段定级，而是检验已有定级是否自洽。**

"隐性推断源"这个术语随之有了严格的、非自造的定义：

> **隐性推断源**：造成定级冲突的、不存在显式计算公式的低级字段组合。

---

## 第二部分：技术靶心——三类推断关系的分离

"是否真的存在推断关系"这个问题，拆开来是三类性质完全不同的东西混在一起：

| 类型 | 成因 | 可判别特征 | 治理含义 |
|---|---|---|---|
| **Ⅰ 公式型** | 定义性恒等式、会计关系、物理定律 | 残差趋于机器零；跨时间、跨地域恒成立 | **已知风险**。规范通常已覆盖（如不会同时发布 LMP 及其三个分量）。方法只需自动检测并剥离 |
| **Ⅱ 机理稳定型** | 调度策略、市场出清、负荷特性在长期运行中形成 | 无显式公式；跨时间窗、跨年份、跨扰动均稳定 | **真正的盲区**。规范失语，本文靶心 |
| **Ⅲ 样本伪相关型** | 时段巧合、共同趋势、季节性、评估协议缺陷 | 换时间窗或换年份即失效 | **误报**。据此管控将造成过度限制，数据无法流通 |

### 全文中心句

> **本文的使命是把第 Ⅱ 类从第 Ⅰ 类和第 Ⅲ 类中分离出来。**

### 这一句话如何串起全部审稿意见

| 审稿意见 | 在新框架下的位置 |
|---|---|
| R1-3「物理定律能推多少」 | 第 Ⅰ 类。方法主动剥离并量化剥离后余量。质疑变成方法步骤 |
| R1-1「术语自造」 | 隐性推断源 = 第 Ⅱ 类，定义可操作 |
| R1-2「低安全推低安全」 | 由保护边界解决（见第三部分），不依赖涉密数据 |
| R1-4「算例未映射到实际结论」 | 输出改为定级冲突 + 处置选项 + 残余风险 |
| R2-5「泛化性与鲁棒性实验不足」 | **跨年、跨窗口稳定性不是补充实验，就是区分 Ⅱ/Ⅲ 的判据本身** |
| R2-2「缺失/坏数据/延迟/口径差异」 | 同上：扰动下推断是否仍成立，是判据的一部分 |
| R2-3「阈值主观」 | 稳定性判据可由置换检验与重采样客观确定 |
| R2-4「多目标耦合不清晰」 | 多个目标共享的推断源 = 优先管控的枢纽字段 |
| R2-1「基线陈旧」 | 唯一需要单纯补做的一条 |

### 为什么这个框架不再"通用"

它把"电力"从背景板变成了约束条件：

- 要识别第 Ⅰ 类，必须懂潮流方程、功率平衡、市场出清机制；
- 要判断什么算"机理稳定"，必须懂电力系统运行规律与市场周期；
- 要给出处置建议，必须对应电力数据的实际发布制度。

换一个行业，这三条约束全部要重写。

---

## 第三部分：数据困难的本质——需要的是一条边界

"用低安全数据推断高安全数据"，本质上需要的不是涉密数据，而是**一条保护边界**。没有边界，推断只是冗余，不构成泄露。

边界有四种来源，均为电力数据共享中真实存在的边界：

| 边界类型 | 含义 | 在 PJM 数据上可否构造 |
|---|---|---|
| **密级边界** | 按分类分级规范映射字段级别 | 可（需人工映射表，最正统） |
| **时间边界** | 发布时延差决定攻击者此刻能拿到什么（日前 / 实时 / 结算后） | 可，需核实 PJM 各数据集发布规则 |
| **主体边界** | 区域可见性，用 A 区数据推 B 区 | 可，且直接对应 R2-2 的省间/地市口径问题 |
| **粒度边界** | 聚合可发布、明细不可发布 | 可（燃料类型明细 vs 总发电量） |

**建议**：密级边界做主线（对应期刊读者语境），再用时间或粒度边界做一次机制性验证，证明结论不依赖于密级标注方式。若能取得南网真实分级数据则升级为主算例——但**本框架的成立不以此为前提**。

---

## 第四部分：必须做的减法

论文当前的问题有一半是贪大。减法比加法更能救它。

| 删减对象 | 理由 |
|---|---|
| **改进门控（相关性驱动 W 向量）** → 降为附录或删除 | 它回答"六类相关性指标各贡献多少"，在新故事里不重要；且它是"方法通用、与电力无关"最有力的证据 |
| **六类相关性初筛** → 从贡献点降为工程步骤 | 压缩篇幅，取消独立图表 |
| **「L1 门控 R² 比全量 DNN 高 0.008」** → 放弃该卖点 | 这是特征选择的卖点不是治理的卖点；且在时序划分下大概率不成立 |
| **12 个目标的大表** → 收缩到 3–5 个 | 每个讲透：泄露后果、推断类型、推断源、处置方式。宽度换深度 |

---

## 第五部分：必须自己划清的界线

**不要声称能自动分类分级。**

定级是行政行为，包含"泄露后果有多严重"这类价值判断，数据本身推不出来。声称自动定级必被攻击且守不住。

准确表述：

> **本文提供的是定级校验器，不是定级器。**

输出定级冲突告警、冲突来源、处置选项、处置后残余风险，把定级决策留给人。承认适用范围，方法反而更可信。

---

## 第六部分：重构后的论文骨架

### 新摘要（草稿）

> 面向电力数据分级发布前的风险审查场景，针对现行逐字段定级无法识别组合推断风险的问题，提出一种推断关系类型判定与推断源定位方法：通过剥离公式型推断、检验统计推断的跨期与扰动稳定性，识别真正构成盲区的隐性推断通道，并定位其最小推断源与阻断集，为定级冲突检测与发布处置提供数据驱动依据。

### 标题方向

现标题全为方法名、无场景。建议向场景与判定靠拢，例如：
- 面向分级发布的电力数据推断风险评估与推断源定位方法
- 电力数据发布前可推断性审查：推断关系类型判定与推断源定位

### 章节结构

```
1 引言
  1.1 电力数据分级共享需求与逐字段定级的局限
  1.2 组合推断风险：一个规范失语的场景
  1.3 相关工作与本文贡献

2 问题定义
  2.1 保护边界与定级自洽性
  2.2 三类推断关系及其治理含义        ← 全文核心
  2.3 判据与评估协议

3 方法（四步对应四个难点）
  3.1 推断能力检验          —— 能不能推（DNN）
  3.2 公式型推断的检测与剥离 —— 是不是常识（恒等式检测 + 领域公式基线）
  3.3 稳定性检验            —— 是不是真的（跨期 / 跨年 / 扰动）
  3.4 推断源定位与最小阻断集 —— 该管谁（L1 门控 + 阻断集）

4 实验
  4.1 设置：数据、边界构造、双评估协议
  4.2 三类推断关系的分离结果            ← 实验核心
  4.3 隐性推断源定位与验证
  4.4 稳定性与扰动鲁棒性
  4.5 基线对比
  4.6 定级冲突与处置建议

5 结论与局限（明确"校验器非定级器"）
```

**新意分布**：3.2 与 3.3 是论文的新意所在；3.1 与 3.4 是已有技术的组织。实验重心放在 4.2——分离前后治理结论的差异。

---

## 第七部分：支撑材料（服务于上述主线的具体实验）

以下实验在新框架中不再是"补充实验"，而是各自服务于主线的某一环。已核实的项目现成资产标注 ⭐。

### 服务于 3.2（公式型剥离）

- ⭐ `inference_path_enumeration_upgrade_proposal.md`「发现 A」已记录：`net_actual_interchange_mw = net_sched_interchange_mw − net_inadv_interchange_mw`，最小二乘残差 RMSE = 3.6e-11（σ_y = 2545）。
- ⭐ `configs/data2025_v2.yaml` 的 `column_combinations` 已在做恒等式排除，但从未在论文中论证。需转为方法的显式前置步骤。
- 需构造领域公式基线：功率平衡、LMP 三分量分解、燃料出力求和。报告其 R²，本文方法的增量 ΔR² 才是贡献。
- ⚠️ **自查项**：`congestion_price_da` 目标 R²=0.9954 / 仅 2 字段，极可能是 LMP 分解恒等式的直接后果（combo 7 中 `system_energy_price_da` 一行被注释）。当前是论文亮点，在审稿人眼中恰是"常识可算"。须先自查再决定去留。

### 服务于 3.3（稳定性检验）

- ⚠️ **必须处理的隐患**：`NEW_PROJECT/src/data_utils.py:127` 使用 `rng.permutation(n)` 随机划分，而目标字段 lag-1 自相关 0.971。项目自查记录显示：仅用前后两小时均值预测中间小时 R²=0.9766，高于 55 字段 DNN 的 0.9685；改时序划分后 Ridge 0.936→0.794，GBDT 0.924→−1.21。当前全部 R² 含乐观偏差。
  **处理方式**：双评估协议，各自对应明确威胁模型——随机划分 = 部分披露场景，时序划分 = 持续发布场景。两套都报，把问题转为贡献。
- 多随机种子：现全文仅 `random_state: 42`，主表须改为 5–10 种子的均值±标准差。
- ⭐ 跨年跨口径：`data/raw/data2023.csv` 与 2025 数据口径本就不同（35 列 vs 70 列，`forecast_load_mw` vs `forecast_load_mw_latest_available`、`actual_flow` vs `net_actual_interchange_mw`、备用类型由聚合改为按类展开）。天然的口径差异案例，无需造数据。
- 扰动鲁棒性：缺失（MCAR/MAR，5/10/20%）、坏数据（尖峰、冻结值 stuck-at、量程越限）、延迟（1h/4h/24h）。判据为选中集合的 Jaccard 稳定性与 R² 保持率。延迟实验有额外价值——它直接验证"延迟发布"作为处置手段的有效性。

### 服务于 3.4（推断源定位与阻断）

- ⭐ `src/knowledge_graph.py`、`test3_Graph.py` 可支撑多目标推断关联图谱：枢纽字段识别（出现在多个目标推断源中的字段，优先管控）、链式推断传递性、多目标最小阻断集。

### 服务于阈值客观化（R2-3）

- 初筛阈值改为置换检验（B=1000 次，取零分布 95% 分位数）+ Benjamini–Hochberg 控制 FDR。六个手工阈值降为一个显著性水平 α。
- ⭐ `scripts/20_lambda_path_scan.py` / `21_lambda_path_scan_v2.py`（v2 已处理尺度补偿与真实退出）可支撑 λ 路径 + 稳定性选择替代固定 λ。
- 达标阈值改为 τ = LCB(R²_full) − ε，只留 ε 一个参数并给敏感性曲线。

### 服务于基线对比（R2-1）

唯一需要单纯补做的一条。现有基线 Lasso(1996)/ElasticNet(2005)/RF(2001)/XGBoost(2016) 确实陈旧。

- ⭐ **D-Gating**（NeurIPS 2025 Spotlight，论文 PDF 在仓库根目录）：`src/models.py:119` `DGatingRegressor` **已实现**，配置中已有 `DGatingDNN`，跑通即可入表，零开发成本。
- STG（ICML 2020，门控类直接竞品）、LassoNet（JMLR 2021）建议补。
- Model-X Knockoffs 可选，附带 FDR 控制，同时回应 R2-3。

---

## 第八部分：待决策事项

1. **保护边界采用哪一种？** 密级映射为主线是默认建议；若能取得南网真实分级数据（哪怕小规模脱敏），则升级为主算例，R1-2 彻底解决。
2. **是否接受重跑全部实验？** 多种子 + 双评估协议意味着表 4–8、图 5–12 全部重做，部分结论（尤其"L1 门控优于全量 DNN"）可能不再成立。但该卖点在新框架下本就应放弃。
3. **修回本刊还是转投？** 两位审稿人均否定定位，通常意味着大修重投。重构后若故事完整，《电力系统自动化》《中国电机工程学报》亦在射程内。
