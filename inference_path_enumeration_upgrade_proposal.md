# 推断路径识别方法升级方案：从"条件替代流水线"到"可认证的路径族枚举"

> 状态：草稿 V0.9（2026-07-26）。基于 6 路文献调研（数据库函数依赖枚举、单调布尔枚举/形式化 XAI、PID/高阶协同、电力隐私应用定位已返回；多重马尔可夫边界、Rashomon/统计控制待融合）。
>
> 本文回答三个问题：①当前方法为什么"笨"、笨在哪里（诊断）；②正确的数学抽象和算法框架是什么（重构）；③如何分阶段落地并冲击顶刊（路线）。

---

## 第零部分：三个实证发现（本次新测，优先级高于后面所有内容）

在设计新方法之前我先跑了几个便宜的核查，结果比预想的严重。**这三条必须先解决，否则后面的枚举框架再严谨也是建在流沙上。**

### 发现 A：数据里存在一个精确的会计恒等式

对全部字段做最小二乘，得到残差 RMSE = **3.6e-11**（机器精度的零），而 y 的标准差是 2545：

$$\texttt{net\_actual\_interchange\_mw} = \texttt{net\_sched\_interchange\_mw} - \texttt{net\_inadv\_interchange\_mw}$$

好消息是 combo 5 已经排除了 `net_sched_interchange_mw`，恒等式被打断。**但这个排除是一个藏在 YAML 里、从未被论证过的人工决定，而它对全部结论有决定性影响。** 论文里必须显式交代：排除了哪些字段、依据是什么（定义性恒等 vs 统计相关）、不排除会怎样。这恰好也是电力审稿人"你找的路径就是恒等式"那一问（风险 R1）的正面回答材料——我们不但知道恒等式存在，还能精确定位并量化它。建议把这套"恒等式自动检测"做成方法的一个前置步骤（最小二乘残差近于零即报警），它本身就是一个卖点。

### 发现 B：训练/测试是随机划分，而数据自相关极强

`src/data_utils.py:126-130` 用的是 `rng.permutation(n)`——**随机划分，不是按时间切**。而目标字段的自相关是：lag-1 = 0.971，lag-24 = 0.776。极端一点说：**仅用前后两小时的均值预测中间那一小时就能得到 R² = 0.9766，比全部 55 字段的 DNN（0.9685）还高。** 随机划分把测试样本夹在训练样本的前后小时之间，模型只要记住"这个 X 模式 → 这个 y 值"就能拿高分。

### 发现 C：换成诚实划分后，当前的 τ=0.95 根本达不到

严格复刻项目预处理（55 字段、按行删 NaN，n=6556）后的对比：

| 划分方式 | Ridge | GBDT |
|---|---|---|
| 随机划分（项目当前做法） | 0.9360 | 0.9240 |
| 按时间划分（前 80% 训 / 后 20% 测） | 0.7942 | −1.2083 |
| 按时间分块 5 折 CV | **0.8637** | 0.0172（各折 0.098/0.095/0.361/0.740/−1.208） |

三点结论：

1. **随机划分显著抬高了 R²**：Ridge 从 0.936 掉到 0.864，GBDT 直接崩掉（树模型对近重复行的记忆最敏感，这本身就是泄漏的指纹）。
2. **τ=0.95 是在被抬高的天花板上刻的**。诚实评估下 Ridge 的上限约 0.86，**τ=0.95 无解——整个路径族会是空的**。也就是说，当前全部"达标"结论都依赖于随机划分带来的乐观偏差。
3. **存在明显的时间分布漂移**：最后一折（最近的时间段）最难，GBDT 在该折为 −1.208。这说明"用历史推断未来"和"在同期内插"是两个难度完全不同的问题。

**但要公允地说：随机划分并非绝对错误，它对应一个特定的威胁模型**——"攻击者已知随机 80% 行的 y，想推断其余 20%"（部分披露场景）。而按时间划分对应"攻击者用历史数据训模型，推断新发布的数据"（持续发布场景，更贴近真实治理需求）。**正确的做法是两个都报，并明确各自的威胁模型假设。** 这反而是一个论文加分项：泄露风险的大小取决于攻击者辅助知识的结构，我们能把这件事量化——目前文献里没人这么做。

**对计划的影响**：这件事的优先级高于枚举框架本身。ε 的定标、路径族的规模、甚至"是否存在达标路径"都取决于先把评估协议定对。所以 V1 的第一件事从"测单调性"改成"定评估协议并重算基线"。

---

## 第一部分：诊断——当前方法的三个结构性问题

### 1.1 现状盘点

当前六阶段流水线（scripts 14–19）：

```
stage01 门控压缩主路径 P → stage02 逐字段三查(C_i / C_i∪R / R) →
stage03 OOF残差 → stage04 dropout显影活跃集 A → stage05 锚定多头找替代组 Q_k →
stage06 回原任务认证+局部去冗余
```

实测数据（run_20260705 系列，55 字段，τ=0.95，R²_full≈0.9685）：

- 主路径 11 字段中 **4 个被 stage02 判为 main_path_redundant**（C_i 自身就 ≥ τ）——stage01 的路径根本没剪到极小；
- 其余 7 个字段全部"条件可替代"，0 个"不可替代"；
- R_only = 0.850 < τ，不存在完全不相交路径。

人工旋钮清单（不完全）：compact_tau=0.95、active_threshold=0.001、obvious_drop_r2、λ 扫描×2、dropout 扫描、mse-tolerance=0.15、min/max-active=3/24、gate-weight=0.5、target-r2=0.95、门控范数比例 threshold-sweep（[0.25,0.80] 共 7 档）、anchor-sensitivity-min=1e-4。**约 12 个耦合旋钮**。

### 1.2 三个结构性问题（对应你提出的三点）

**问题一：发现与判定混杂（"笨"和"阈值多"的根源）。**
门控值在流水线里承担了两种角色：既用来*发现*候选字段（显影、锚定），又用来*判定*成员资格（active_threshold、threshold_ratio 卡门控范数）。第二种用法是一切阈值堆积的来源——门控值是训练副产物，它的数值大小没有统计语义，卡在 0.25 还是 0.35 只能靠扫描。正确的架构原则是：

> **启发式只负责"提议"（提候选、排顺序），一切"判定"回到达标函数 F(S) 的直接评估 + 统计检验。**

判定一个字段是否在替代组里，唯一严谨的方式是把它删掉重测 F，而不是看它门控值多大。当前方法其实在 stage06 已经这么做了（回原任务认证），问题是前面各阶段的阈值判断仍然在过滤候选、影响最终结果。

**问题二：输出对象选错了（"替代组不可组合"的根源）。**
当前输出是"主路径 P + 每个字段 x_i 的替代组 Q_i"。这是一个以 P 为中心的局部视图。你观察到的现象——A 缺失可用 CD 补（因为 C 与 B 协同），但 A、B 同时缺失时 CD∪EF 未必行——在这个视图下是反常，但在正确的视图下是**必然**：

"Q_i 替代 x_i"这句话的严格含义是"存在一条不含 x_i 的达标路径 P' ⊆ (P∖{x_i})∪Q_i"，而这条 P' 里**包含 B**。B 也缺失时 P' 被破坏，所以组合失效。替代关系从来就不是字段对字段的，而是**路径对路径**的。

正确的原生输出对象是两个集合族：

- **M = 极小达标集族**（所有推断路径）：F(P)=1 且删任一字段就不达标；
- **N = 极大非达标集族**：F(S)=0 且加任一字段就达标。

一切下游问题都变成这两族上的查询，不再有组合爆炸：

| 问题 | 在 (M,N) 上的查询 |
|---|---|
| 字段集 M₀ 同时缺失后还能否推断？ | 一次 oracle 调用 F(U∖M₀)；或查 M 中是否有成员与 M₀ 不交 |
| 缺失后用什么恢复？ | M 中与 M₀ 不交的成员（极小性自动继承，无需重算） |
| x_i 是否不可替代（必经源）？ | x_i 是否出现在 M 的所有成员中 |
| 屏蔽哪些字段能阻断所有推断？ | M 的极小命中集（见 2.4 对偶定理，可直接枚举无需先枚举 M） |
| 是否存在完全不相交的两条路径？ | M 上的最大不相交子族（匹配数 ν） |

**问题三：完备性没有定义（"达不到上限/恢复的不是原来那部分"的根源）。**
当前方法没有终止判据——跑完六个阶段就停，找到多少算多少，无法回答"还有没有别的路径"。而"恢复的是不是原来的信息"这个担忧，本质上是因为方法声称的东西（替代）和实际验证的东西（达标）之间有语义缝隙。修复方式有两层：

1. **语义澄清**：方法应声称的是*性能等价*——"(C_i∪Q_i) 达到与全字段模型统计不可区分（差距 ≤ ε）的精度"——而不是"Q_i 恢复了 x_i 携带的那部分信息"。前者是治理场景真正需要的（攻击者不在乎恢复的是哪部分信息，达标就是泄露），后者需要信息论工具单独刻画（见第四部分 PID 分析，作为可选的解释层）。
2. **阈值锚定**：τ 不再是拍脑袋的绝对值 0.95，而是 **τ = LCB(R²_full) − ε**：先用多种子把全字段模型的 R² 钉死（均值+下置信界），τ 定义为它减去唯一的容差参数 ε。这样"主路径达不到上限"的缝隙被 ε 显式吸收，且 ε 可以做敏感性曲线（对应 λ 路径扫描的分级思想）。

---

## 第二部分：重构——单调达标函数上的可认证枚举框架

### 2.1 语义层：把问题钉死（含对本方案早期版本的一处纠错）

> **纠错说明**：本节初稿写的是 `F(S)=1 ⟺ LCB(R̂²(S)) ≥ τ, τ = LCB(R̂²_full) − ε`。这个写法**统计上站不住**——R̂²(S) 与 R̂²_full 在同一份数据上估计、高度正相关，把两者拆成"点估计 vs 阈值"比较会丢掉相关性，得到的检验既非名义水平 α，也没有方向保证。正确做法是**直接对差值做推断**。下面是修正版。

**定义（对齐 Williamson et al., JASA 2023 的 algorithm-agnostic variable importance）**

$$\psi(S) := R^2_{\text{full}} - R^2(S), \qquad F(S)=1 \iff \psi(S) \le \varepsilon$$

这个参数与 Williamson 框架里的 oracle predictiveness 差值**一比一对应**，而且他们的 $f_{0,s}$ 定义就是"忽略某些特征后**重新最优化**得到的 oracle"——正是我们的重训练语义（不是把输入 mask 掉），不需要任何改写就能直接引用其理论。差值的影响函数天然吸收了两项的相关性，这是修正的关键。

**假设方向必须翻转（这是当前 0.95 规则的本质错误）**

- H₀: ψ(S) ≥ ε（该子集的性能损失不可接受）
- H₁: ψ(S) < ε（该子集非劣）
- **拒绝 H₀ 才判 F(S)=1。**

为什么这个方向才对：现在的做法是"R̂²(S) ≥ 0.95·R̂²_full 就算达标"，等于把"非劣"放在原假设上、不显著就接受——这是统计上典型的**用不显著证明等价**。翻转之后，证据不足默认判 0，而被 α 控制的第一类错误恰好是"错误地宣布一个坏子集合格"——这正是治理场景下最该防的错误方向（漏报泄露风险比虚报更危险）。

**判定的七步实现**（可直接编码，估计量与方差都有闭式）

1. K 折交叉拟合（K=5 或 10）。**时序数据必须用 blocked / rolling-origin 划分**，不能随机切（见坑 ①）。每折的评估集再切成互不相交的 A、B 两半，对应训练也不共用——这是 Williamson 的 sample splitting，用于对抗 ψ=0 处的退化。
2. 在 A 的训练部分训 $\hat f_{\text{full}}$，在 B 的训练部分训 $\hat f_S$。
3. 评估时**分母固定用全体数据的 Var(Y)**（不可每个子集用各自评估集的方差，否则 R² 不可比）。
4. 点估计 $\hat\psi(S) = \hat v_{\text{full}} - \hat v_S$。
5. 方差用逐样本影响函数：$\hat\varphi_{\text{full}}(z_i) = \frac{1}{\hat\sigma^2}[-(y_i-\hat f_{\text{full}}(x_i))^2 + (1-\hat v_{\text{full}})(y_i-\bar y)^2]$，$\hat\eta^2 = \text{mean}(\hat\varphi^2)$，则 $\hat\omega(S) = \hat\eta^2_{\text{full}}/n_A + \hat\eta^2_S/n_B$。
6. 检验量 $T(S) = (\varepsilon - \hat\psi(S))/\sqrt{\hat\omega(S)}$；$T > z_{1-\alpha}$ 判 F(S)=1。等价形式：单侧上置信界 $\text{UCB}_{1-\alpha}(\psi(S)) < \varepsilon$。单侧 p 值 $= 1-\Phi(T)$。
7. 判不出的（UCB 跨过 ε）标为**灰区**，追加种子重测；仍灰则写入报告的不确定集合，**不参与任何剪枝与对偶推理**。灰区规模本身是要报告的诊断量——它大说明 ε 定得太靠近噪声带。

**ε 怎么定（唯一的容差参数，三种定标依据按推荐度排序）**

1. **应用效用定标（首选）**：由下游可接受损失反推。电力场景可把 ψ=ε 映射成 RMSE 增幅，再折算到调度/备用容量成本。这是临床非劣性试验的标准做法——**margin 必须由领域知识定，不能由数据定**，否则检验水平失真。
2. **噪声地板定标**：ε = c·ŝe(R̂²_full)，c ∈ {1,2}，含义是"损失不超过全模型 R² 自身估计不确定度的 c 倍"。须在独立定标子集上算好 ŝe 后**冻结**，避免 margin 与检验统计量相关。
3. **兼容旧口径**：ε = 0.05·LCB_α(R²_full) 可近似复现当前的 0.95 规则，便于新旧结果对照。

辅判据：同时要求 R²(S) 的下置信界 > 0，防止 R²_full 本身很小时出现"人人非劣"的退化。

**多重比较**：我们要对成百上千个子集反复判定，必须处理。**首选"搜索/确认分离"**——搜索集上跑全部枚举与剪枝，**完全不做检验、不报 p 值**；只在独立的确认集上，对最终入围的少数（≤10 条代表性路径）做上述非劣性检验 + Holm 校正。这把多重比较从上千降到个位数，同时规避了选择后推断的全部麻烦。备选：p 值走 BH（嵌套子集通常满足 PRDS）；或利用单调性 S⊂S′ ⟹ ψ(S)≥ψ(S′) 做 closed testing 沿链传播判定，不额外付 α。

**单调闭包修复**：有限样本下 R̂² 可能违反单调性（加字段反而掉点）。定义 $\tilde v(S)=\max_{S'\subseteq S,\,\text{已测}} \hat v(S')$ 强制单调，使格搜索根基成立。实测中必须**记录单调性违例率**——这是审稿人必问的数据，也是 V1 第一步要测的东西。

这套定义下，**全流水线只剩 3 个有语义的参数**：容差 ε、置信水平 α、oracle 预算 B。原来的 12 个旋钮中，门控相关的全部降级为搜索启发式（影响找得快慢，不影响结论对错）。

### 2.2 成本模型（实测，非估计）——这一节推翻了一个常见误判

文献调研全都假设"oracle 分钟级、总预算 10²–10³ 次"，据此设计了复杂的代理模型层。**但本项目的实测成本远低于此**：

从 stage02 运行记录反推（`01_precheck` 目录 24 个训练案例，22:05:11→22:09:29 共 258 秒，4 GPU × 4 worker）：

| 指标 | 实测值 |
|---|---|
| 单次子集训练（64-32-16 MLP，8760 样本，160 epoch/patience 35） | **≈ 43 秒** |
| 4 卡并行吞吐 | **≈ 5.6 次/分钟 ≈ 335 次/小时 ≈ 8000 次/天** |
| 带 3 种子 CI 的有效判定吞吐 | ≈ 110 次判定/小时 ≈ 2700 次/天 |

**这个数字改变三件事**：

1. **完备枚举从"奢望"变成"一两天的机时"**。若路径族 |M|+|N| 在数百量级，每条路径 shrink 约 15–25 次 oracle，总量约 10⁴ 次 → **约 1.2 天**。这是可以真跑的，不是纸上谈兵。
2. **小规模穷举对照实验（审稿人必问的 recall 证据）完全负担得起**：12 字段全穷举 2¹²=4096 次单种子 ≈ 12 小时；15 字段 2¹⁵=32768 次 ≈ 4 天（周末机时）。可以给出**真实的完备性 recall/precision 曲线**而不是口头承诺。
3. **代理模型层从"生命线"降级为"优化项"**。V2 可以直接用 DNN oracle + 缓存 + 单调剪枝，不引入 LightGBM 代理——**少一个组件就少一层近似误差和一批调参旋钮**，这本身就是在回应"方法太笨太复杂"。代理层留到 V3 若预算吃紧再加。

顺带一个观察：当前流水线跑完 stage02 花了 36 分钟，但真实计算量只有约 30 次训练（≈5 分钟机时）。**大部分墙钟时间消耗在六个脚本之间的人工编排与 JSON 交接上**，不是算力。统一枚举器不仅更严谨，也会更快。

另需澄清一个参数事实：当前代码里 `tau=0.95` 是**绝对阈值**而非 0.95·R²_full。由于 R²_full=0.9685，实际相当于 0.981·R²_full，容差 ε 仅 0.0185——比"0.95 倍"的写法严格得多（后者对应 τ=0.920）。这个差别显著影响路径族大小，论文里必须写清，并做 ε 敏感性曲线。

### 2.3 算法层：oracle 引导的双边界枚举

这是文献调研的最大收获：**这个问题在三个社区被独立解决过，算法骨架收敛到同一个**——

- 数据库社区：近似函数依赖/唯一列组合枚举（TANE→DFD→Pyro→HyFD，成本锚定在边界族大小而非 2^p）；
- SAT 社区：MUS/MCS 枚举（MARCO：map-solver 维护未探索区域 + grow/shrink）；
- 形式化 XAI：**单调分类器的 AXp/CXp 枚举（Marques-Silva et al., ICML 2021）——与我们的问题几乎逐点同构，有正确性/完备性定理和官方代码 XMono**。

映射表：推断路径 = minimal true point = MUS = AXp（充分理由）；极大非达标集 = MSS；极小阻断集 = MCS = CXp。

**主循环**（ICML'21 Algorithm 3 的回归适配版，每个极小集只花 1 次 SAT 调用 + O(k log p) 次 oracle）：

```
Map ← ⊤        # 55 个布尔变量上的 SAT 公式，模型 ↔ 未探索子集
M, N ← ∅, ∅
while SAT(Map) 且预算未尽:
    seed ← 从 Map 取种子（小端优先，见下）
    if F(seed) = 1:
        P* ← shrink(seed)            # QuickXplain 分治 + 代理模型排删除顺序
        M ← M ∪ {P*};  Map ← Map ∧ (∨_{i∈P*} ¬x_i)      # 屏蔽 P* 的所有超集
    else:
        S* ← grow(seed)              # 贪心加字段保持不达标，代理排序
        N ← N ∪ {S*};  Map ← Map ∧ (∨_{i∉S*} x_i)       # 屏蔽 S* 的所有子集
终止: Map 不可满足 ⟹ (M,N) 完备（相对 oracle 的数学证明）
中断: 输出部分 (M,N) + Map 剩余区域作为"未探索证书"（anytime 性质）
```

关键工程决策（各调研报告的坑都吃进来了）：

1. **种子从小端取**（Pyro 的 ascend 而非 MARCO 原版的极大种子）：从小集合出发向上爬到刚好达标，避免"从 55 字段全集往下 shrink"的 oracle 浪费——这是 MUS 文献里明确批评过的大种子陷阱。

2. **种子来源就是现有流水线的产物**（这是把已有资产接进新框架的关键，也是最省力的一步）。多样化种子有四个现成来源，全部零新增开发：
   - stage01 的 D-Gating 在**不同 λ、不同随机种子**下压出的多条不同主路径——每条都是一个高质量"峰"；
   - λ 路径扫描（已交付专利 V2 的产物）沿路径给出的**分级退出点**，天然是一串由紧到松的候选集；
   - stage04 dropout 显影出的活跃集 A；
   - stage05 锚定多头的各头输出 Q_k。

   **它们全部降级为"提议器"，不再承担任何判定职责**——判定统一归 oracle + 统计检验。这一句话就消掉了 active_threshold、threshold_ratio、anchor-sensitivity-min 等一整批阈值：这些参数现在只影响"提议得好不好"（找得快慢），不影响"结论对不对"。**这正是消除"机械感"的核心手术。**

3. **单调界传播 + memoization（性价比最高的一条）**：所有已测 (S, R̂², CI) 入 trie；新候选先查上下锥——已有子集达标→免测判达标，已有超集不达标→免测判不达标。文献报告可省 30–70% 调用，且实现简单、无近似误差。

4. **shrink 用 QuickXplain 分治**（Junker AAAI 2004）：单条路径的 oracle 数从 ≤55 降到 O(k·log(55/k))，k≈5–10 时约 15–25 次。删除顺序用门控范数升序（先试着删门控最小的），命中率高时还能更省。

5. **代理模型层：V2 先不做**。基于 2.2 节的实测成本，DNN oracle 本身就够快，引入 LightGBM 代理换来的加速不足以抵消它带来的近似误差、校准工作量和额外旋钮。**留作 V3 的可选优化**（若实测发现 |M|+|N| 超出预期一个数量级再启用），届时按 Pyro 的做法：代理估计落在 τ±margin 才升级精测，并先用 30–50 个随机子集校准秩相关。

6. **灰区判定的预算策略**（消除"卡阈值"任意性的关键，必须写死成规则而非人工裁量）：
   - 默认每个子集 3 个种子，按 2.1 第 5–6 步算 ψ̂ 与 UCB；
   - UCB 明确落在 ε 一侧 → 定案；UCB 跨过 ε → 升级到 9 个种子重算；
   - 9 种子后仍跨 → 标记为**灰区**，写入报告的不确定集合，**不参与任何剪枝与对偶推理**（形式化 XAI 调研明确警告：让不确定判定参与单调剪枝会污染完备性）；
   - 灰区规模本身是要报告的诊断量——它大，说明 ε 定得太靠近噪声带。
   - **注意搜索阶段与确认阶段的分工**（2.1 多重比较）：枚举搜索时用点估计快速推进、不报 p 值；只有最终入围的代表性路径才在独立确认集上做正式非劣性检验。搜索阶段的"判定"是工作假设，确认阶段的判定才是论文结论。

7. **预算规划**：查询下界是 |M|+|N|（定理，任何 membership-query 算法都绕不过）。按 2.2 的吞吐，10⁴ 次调用约 1.2 天，可支撑数百条路径的完备枚举。若解族真的爆炸（电力字段强相关，Pyro 在 35 列上曾找到 10.8 万条近似依赖），降级预案：只完备枚举尺寸 ≤k 的路径与阻断集，输出"更大集合未探索"的显式声明 + Map 剩余区域证书；anytime 性质保证随时可交付。

### 2.4 对偶输出：极小阻断集（治理层的直接答案）

形式化 XAI 调研带来的最优雅结果——**AXp/CXp 对偶定理**（Reiter 1987 → Ignatiev et al. 2020）：

> 极小阻断集族 = 推断路径族的极小命中集族，反之亦然。

两个直接推论：

1. **阻断集可以直接枚举**，不必先枚举所有路径再解命中集（findCXp 一次 ≤2p 个 oracle 调用）。治理方最关心的"屏蔽哪 2–3 个字段就能断"可以按尺寸从小到大先出（OptUx 思路）。
2. **最优屏蔽方案不需要完整枚举**：implicit hitting set 循环（MaxHS 范式）——解当前已知路径族的最小(加权)命中集 H（ILP，55 字段秒级）→ 测 F(U∖H)：不达标则 H 已证最优，达标则从 U∖H 里 shrink 出一条新路径加入，循环。典型几十轮收敛。

这把项目从"路径识别"升级为"路径识别 + 最优阻断"闭环，输出直接对接数据分级分类的治理动作（可发布/需脱敏/禁止组合发布）。

### 2.5 多字段缺失：从排列组合到族上查询

你的痛点 2 在新框架下的完整答案：

- **判定层**（是否可恢复）：F(U∖M₀) 一次调用，或查 M 中不交成员——零组合爆炸；
- **构造层**（用什么恢复）：M 中与 M₀ 不交的成员就是恢复方案，极小性自动成立；
- **解释层**（为什么单字段替代组不能拼）：见第三部分的组合缺陷量与替代依赖图。

注意一个微妙点：**逐字段替代组 Q_i 仍然值得输出**（它是路径族在"删 x_i"切面上的投影，业务上直观），但它从"原生结果"降级为"派生视图"——从 M 里算出来，而不是单独训练出来。这样它们之间的一致性自动成立。

### 2.6 残差谱系：把"恢复的不是原来那部分"变成可测量的量（原创诊断）

你的痛点 3 值得一个比"语义澄清"更硬的答案。担忧的实质是：路径 P 和替代路径 P′ 都达标（R² ≥ τ），但它们可能是靠**不同的信息机理**达标的——P′ 补回的未必是 P 里 x_i 贡献的那部分。第 2.1 节的性能等价语义回避了这个问题，这里给出**直接测量它**的办法，且几乎零额外成本（复用 stage03 已有的 OOF 残差机制）。

对每条已认证路径 P_k，取其 out-of-fold 残差向量 e_k = y − ŷ_{P_k}。定义路径间的**机理相似度** ρ_{kl} = corr(e_k, e_l)。含义很直观：残差是"模型没解释掉的那部分 y"，两条路径若在**同样的样本上失败**，说明它们抓的是同一份信息；若失败模式不同，说明推断机理确实不同。

由此得到三个可直接写进论文的诊断量：

1. **替代保真度** ρ(e_P, e_{P′})：主路径与其替代路径的残差相关。ρ 高 → 替代是"同机理复现"，你的担忧不成立；ρ 低但双方都达标 → **替代确实换了一套机理**，你的担忧被证实且被量化。无论哪种结果都是有价值的发现，而不再是悬而未决的疑虑。
2. **机理聚类**：对 {e_k} 做相关矩阵聚类，把数百条路径归并成少数几个**推断机理族**。这一步同时解决"路径太多没法读"的可用性问题——治理报告呈现的是 5–8 个机理族及其代表路径，而非几百条清单。这是很好的论文主图。
3. **互补性检验**：若 R²(P_k ∪ P_l) 显著高于 max(R²(P_k), R²(P_l))，说明两条路径信息互补（残差低相关的必然推论）。互补路径对的存在意味着**攻击者组合多条路径能超过任何单条路径的精度**——这是治理侧一个此前没人报告过的风险量。

这套分析的成本：每条路径一次 OOF 残差（枚举时顺带保存即可），之后全是向量相关运算，秒级。**性价比极高，且直接把你原本的疑虑转化为方法的一个卖点**。

---

## 第三部分：协同结构——"替代不可组合"的严格刻画（论文创新点）

调研确认：**"特征替代组的可组合性"在文献中是空白**。最接近的理论（submodularity ratio、gross substitutes）只给出"什么条件下可组合"，而这些条件恰好在存在协同/压制变量时失效——没有免费定理，只能检测。这正好是我们的机会：把检测本身做成方法论贡献。

### 3.1 组合缺陷量 D_ij

对主路径字段 i, j 及其替代组 Q_i, Q_j，定义：

D_ij = [v(P∖i ∪ Q_i) − v(P)] + [v(P∖j ∪ Q_j) − v(P)] − [v(P∖{i,j} ∪ Q_i ∪ Q_j) − v(P)]

D_ij ≈ 0：两个替代独立可加，组合安全；D_ij 显著为正：组合失效。失效必属两类之一：

- **(a) 协同型**：Q_i 中某字段 c 与 x_j 是超模对（Δ_{c,x_j}v > 0；PID 语言：c 的补偿信息主要落在 Syn(c, x_j; y) 原子上，x_j 撤场该原子蒸发）——这就是你说的"C 和 B 有协同"；
- **(b) 冗余型**：Red(Q_i, Q_j; y) 大——两个替代组从同一外部信息源取水，合并后总量不够补两个缺口。

### 3.2 替代依赖图：把 2^|M₀| 组合坍缩成图查询

对每个替代组 Q_i 和每个上下文字段 B ∈ P∖{x_i}，判定 Q_i 的有效性是否依赖 B 在场（判定量：v((P∖{x_i,B})∪Q_i) 掉到 τ 以下且掉幅显著超过基线）。所有依赖关系构成有向图 H：**Q_i → B 表示"该替代方案要求 B 在场"**。

多缺失集 M₀ 的替代方案可行性 = 图上检查：(i) 所选各 Q_i 的依赖字段与 M₀ 不交；(ii) M₀ 内每对的组间冗余核查通过。只有图判定"存疑"的组合才消耗真实训练预算。

### 3.3 三层检测漏斗（预算从 C(m,2)×3 压到 O(m) 次重训练）

1. **零成本层**（一次性半天）：高斯 copula O-information 及其梯度（HOI 包，全部 ~19,600 个三元组 (x_i,x_j,y) 分钟级）、NID 从已训 DNN 权重解析交互、EBM/FAST 成对交互、MMI-PID 协同原子闭式解 → 输出 55×55 协同热力图 + 每字段"协同枢纽度"。**协同枢纽度高的字段就是治理上的"关键在场字段"**——多个替代路径都依赖它，单点管控收益最大。这本身就是一个可发表的新指标。
2. **代理层**：LightGBM v̂ 秒级测所有候选 (i,j) 对的 D̂_ij；
3. **精测层**：只对代理判定落入 τ±margin 的对做 DNN 重训练（3 种子+CI）。

注意坑：NID/shapiq 等解释的是"当前这个模型"（掩码博弈），我们的 F 是"数据里有多少信息"（重训练博弈）——前者只能当优先级先验，达标性结论必须落在重训练 R² 上。

---

## 第四部分：与三个痛点的逐条对账

| 你的原话 | 根因 | 方案对应 |
|---|---|---|
| "方法笨、阈值多、机械" | 发现与判定混杂；门控值被赋予它不具备的判定语义 | 2.1 三值统计判定 + 2.2 启发式降级为提议器；12 个旋钮 → ε/α/B 三参数 |
| "单字段替代组不能排列组合到多字段缺失" | 替代是路径对路径的关系，不是字段对字段的 | 2.4 族上查询（判定/构造零组合爆炸）+ 3.1–3.3 组合缺陷与依赖图（解释+论文创新） |
| "主路径达不到上限，恢复的可能不是原来那部分" | 声称（信息恢复）与验证（性能达标）语义缝隙；τ 无锚定 | 2.1 τ=LCB(R²_full)−ε 锚定 + 性能等价语义澄清；可选 PID 缺口重叠分析作解释层 |
| "尽可能找到所有路径，或证明接近" | 无终止判据、无完备性定义 | 2.2 Map-UNSAT 完备性证书（相对 oracle）+ anytime 部分枚举 + 小规模(≤15字段)穷举对照实验测 recall |

---

## 第五部分：落地路线图

> 机时预算参照 2.2 实测：约 5.6 次训练/分钟（4 卡并行）。下面每步都标了估算机时，便于排期。
>
> 运行环境已确认就绪：`conda activate Pypower`（torch 2.7.1+cu118，4 GPU 可见）或 `Pytorch310_codex`（torch 2.5.1+cu124，4 GPU）。注意 `SCRIPT_USAGE.md` 里写的 `Pytorch310` 这个环境名当前不存在，实际可用的是上述两个——V1 开跑前先统一好，免得踩空。

### V1（1–2 周，不推翻现有产物，先把地基验平）
0. **【已完成，见第零部分】划分方式体检** —— 已确认是随机划分且影响重大。
0b. **定评估协议并重算基线（现在的第一优先级，约 3 小时机时）**：
   - 在 `prepare_supervised_dataset` 增加 `split_mode` 参数，支持 `random` / `temporal` / `blocked_cv`（后者用 purged/embargo 边界，丢弃块交界处 ±24 小时防泄漏）；
   - 用 DNN 在两种口径下重算 R²_full，得到诚实的天花板；
   - **据此重定 ε**（不能再用 τ=0.95 这个在虚高天花板上刻的线）；
   - 产出一张"随机 vs 分块"的对照表，两个威胁模型各报一套结果。这张表进论文的实验设置节。
1. **单调性违例率测量**（约 2 小时机时）：随机抽 40 对嵌套子集 (S ⊂ S′)，每个 3 种子测 R̂²，统计违例率与幅度。这是整套格搜索理论的地基——若违例率高，2.1 的单调闭包就不是保险措施而是必需品，方案细节要相应调整。**注意必须在新评估协议下测**，否则测的是泄漏后的假单调性。
1b. **恒等式自动检测（约 10 分钟，纯 CPU）**：对候选字段集做最小二乘/秩检验，自动标出残差近于零的精确线性恒等式，形成"定义性字段"清单并显式排除。把当前藏在 YAML 里的人工排除决定升级为方法的一个可复现步骤。
2. **统计化改造**（约 4 小时机时）：把 stage02 的判定换成 2.1 的 ψ(S) 非劣性检验（差值推断 + 翻转假设 + 3/9 种子升级 + 灰区标记）。重点复核那 4 个 `main_path_redundant` 字段——它们的 C_i 值（0.9549/0.9646/0.9571/0.9579）离 τ=0.95 只有 0.005–0.015，**极可能落在灰区**，当前的单次训练判定不可靠。这一步很可能直接改写现有结论。R 包 `vimp`（`cv_vim(type="r_squared", sample_splitting=TRUE)`）已实现同款估计量与方差，可用来交叉验证我们自己的实现是否算对。
3. **二阶缺失动机实验**（约 6 小时机时）：先算零成本协同热力图（3.3 第一层，纯 CPU 分钟级），挑 top 10–15 对做 P∖{x_i,x_j} 实测 —— **用数据证明"替代不可组合"现象真实存在**。这是后续一切故事的动机实验，也是论文的第一张关键图。

### V2（3–6 周，核心重构）
4. 实现双边界枚举器：oracle 复用现有训练代码 + trie 缓存 + 单调闭包；Map 用 PySAT；shrink 用 QuickXplain + 门控排序；种子来自 2.3 第 2 条的四个现成来源。参考 XMono（ICML'21 官方代码）。**不引入代理模型**（见 2.3 第 5 条）。
5. **穷举对照验证**（约 12–24 小时机时）：先在 12 字段子问题上做 2¹²=4096 全穷举，测枚举器的 recall/precision；有余力再上 14–15 字段。这是回应"完备性凭什么"的硬证据。
6. 跑全 55 字段完整 (M,N)（估算 1–2 天机时），产出路径族、极大非达标族、Map 剩余区域证书。
7. 对偶输出：按尺寸枚举极小阻断集 + implicit hitting set 求加权最优屏蔽方案（ILP 秒级，oracle 每轮 ≤55 次）。
8. 残差谱系分析（2.6）：路径聚类成机理族 + 替代保真度 + 互补性检验（近乎零成本）。

### V3（1–2 月，论文冲刺）
9. 替代依赖图 + D_ij + 协同枢纽度全量分析。
10. 论文实验矩阵（应用定位调研的 P0/P1 清单）：imputation 基线（ΔR²）、多模型族 oracle（Ridge/GBDT/RF/DNN 取 max）、τ/ε 敏感性曲线、时间切分验证、外部旁道知识（气象/日历/价格）鲁棒性、与 SES/TIE*/Boruta/knockoff/SHAP-topk 基线对照、阻断集 vs DP 加噪的隐私-效用 Pareto。
11. **物理恒等式分层**（TSG 审稿人致命一问的解药）：把找到的路径分为"物理恒等类"（净交换 ≈ 发电 − 负荷 ± 联络计划，作方法正确性 sanity check）与"统计非平凡类"（卖点），量化后者占比。

### 投稿定位（应用调研结论）
- 🥇 IEEE TSG：故事="CEII/数据分级只给定性清单、DP 只管加噪、'哪些字段能一起发'无人回答"；
- 🥈 TKDE：需补完备性定理+复杂度+对偶定理的形式化（V2 的产出正好够）;
- 招牌指标：**不相交路径数 ν(H)**（leakage redundancy，文献无对应物）、最小阻断代价、不可替代指数、协同枢纽度；
- Related Work 必须主动对标四支近邻（VLDB'22 full deniability、ICML'21 单调分类器解释、TIE*/SES、CCS'17 proxy use）并给差异表——差异点：统计 oracle（非逻辑依赖）+ 数据集级（非实例级）+ 对偶阻断 + 可替代性分级 + 不相交性判定的组合，没有已有工作同时具备。

---

## 第六部分：自我批判（本方案自身的风险与对策）

0. **优先级最高的风险已经落地成事实**：第零部分的发现 B/C 表明，评估协议本身就有问题，且当前 τ 在诚实划分下无解。任何在旧协议上继续堆砌的方法学都会被审稿人一击穿透。**先修评估协议，再谈枚举。**
1. **最大的单点风险：F 可能根本不够单调。** 整套格搜索、剪枝、对偶都建立在单调性上。8760 个样本、55 维、小 MLP，训练方差完全可能让"加字段掉点"频繁发生。V1 第 1 步就是测它，**测完再决定方案是否需要结构性调整**（若违例率高，可能要改用固定架构+更多种子，或干脆把 oracle 换成方差更小的 GBDT 作主判定器、DNN 作复核）。这一条排在第一位，因为它可能推翻后面所有设计。
2. **解族爆炸使输出失去可读性**：数千条路径没人看。对策：2.6 的残差机理聚类是主要解药（几百条 → 5–8 个机理族）；辅以必经源/高频字段/尺寸分布/ν(H) 等聚合统计，完整族作附录数据。
3. **新颖性被 ICML'21 覆盖的风险**：算法骨架确实来自他们（必须引用，不能藏）。我们的增量在：回归 + 有噪声重训练 oracle（他们是确定性分类器上的固定模型）、灰区三值处理、残差机理谱系、协同/组合缺陷分析、电力治理落地。写作时把贡献重心放在后四者，**不要把枚举骨架包装成主要创新**——那样反而会被审稿人一击即中。
4. **oracle 成本仍可能超预算**：2.2 的实测给了乐观基线，但若 |M|+|N| 达数千，仍会吃紧。对策：先跑 20–30 轮观察 |M|、|N| 增长曲线外推总量，再决定是否启用代理层或降级为尺寸受限枚举。**不要一上来就跑全量。**
5. **λ 路径扫描专利（已交付 V2）与本方案的关系**：λ 扫描给的是单路径上的分级退出点，本方案给的是路径族——正交且互补（扫描可作为种子来源与 shrink 排序依据），不冲突。若本方案成型，路径族枚举 + 对偶阻断本身可以是**第二篇专利**的主题。
6. **"统计阻断 ≠ 绝对阻断"**：屏蔽后 R²<τ 只对当前模型族与当前数据成立。表述纪律：一律写"将可推断精度压至 R² < τ（在模型族 𝓕 与数据 D 下）"，并用多模型族取 max 增强结论强度。
7. **本方案自身别变成新的"笨"**：新框架引入了 SAT/trie/QuickXplain 等组件，复杂度是往上走的。守住的原则是——**复杂度可以在搜索里，不可以在判定里**。判定路径必须始终是"训模型 → 算 CI → 比 τ"这一条，任何时候都能一句话讲清某个结论是怎么来的。若发现某个结论需要解释三层启发式才能说清，那就是又滑回老路了。

---

## 附 A：调研覆盖情况与一个已知缺口（如实记录）

本方案基于 6 个方向的并行文献调研，其中 5 个拿到完整报告并已融合进正文：

| 方向 | 状态 | 主要贡献到本方案的部分 |
|---|---|---|
| 数据库函数依赖 / UCC / 拟标识符枚举 | ✅ | 2.3 双边界枚举骨架、剪枝与预算规划 |
| 单调布尔枚举 / 超图对偶 / MUS / 形式化 XAI | ✅ | 2.3 主循环、2.4 对偶阻断、完备性证书 |
| PID / 高阶交互 / 协同效应 | ✅ | 第三部分全部（组合缺陷量、替代依赖图、三层漏斗） |
| Rashomon / 统计误差控制 | ✅ | 2.1 全部（含对早期错误写法的纠正） |
| 电力隐私推断与顶刊定位 | ✅ | 第五部分投稿定位、实验矩阵、审稿风险清单 |
| 多重马尔可夫边界 / 统计等价特征集 | ⚠️ **部分** | 见下 |

**已知缺口（不影响主线，但补上会更完整）**：多重马尔可夫边界方向的 agent 两次运行都卡在检索循环里未能产出报告。该方向的核心结论已由其他三份报告交叉覆盖（TIE\* 的定位、SES 的等价签名概念、与我们方法的差异对比），**缺的是细节层**：

- [ ] Borboudakis & Tsamardinos, DMKD 35(4):1393–1434, 2021《Extending greedy feature selection algorithms to multiple solutions》——需要确认：**集合级等价（一组字段等价于另一组）是否被处理，还是仍依赖"单字段等价类做笛卡尔积"的假设**，以及"识别所有解"的定理条件。这一点直接决定我们"替代组"贡献边界的表述强度，**动笔写论文前必须核实**。
- [ ] SES/MXM (Lagani et al., JSS 80(7), 2017) 的统计等价检验细节与多重比较校正方式。
- [ ] Bach 2024《Finding Optimal Diverse Feature Sets with Alternative Feature Selection》（IJDSA）——已知其证明问题 NP-hard 并给出整数规划表述，可作为 V2 枚举器的对照基线。

补法建议：这三篇都有开放获取版本（PMC8550441 / JSS 官网 / arXiv:2307.11607），直接读原文比再派 agent 可靠。

## 附 B：本次会话产生的调研报告存档

五份完整报告存放于本次会话的 scratchpad 目录，含论文清单、算法伪代码、开源实现指针与坑位清单，写论文 Related Work 时可直接取用：

- `report_2_fd_ucc.md` — 数据库极小集枚举
- `report_3_monotone_xai.md` — 单调枚举与形式化解释（含 AXp/CXp 对偶）
- `report_4_rashomon_stats.md` — 统计检验与误差控制（含对本方案的纠错）
- `report_5_synergy_pid.md` — 协同效应与组合失效
- `report_6_power_positioning.md` — 电力应用版图与投稿定位

> 注：scratchpad 是会话级临时目录。若要长期保存，建议复制到 `NEW_PROJECT/docs/` 下随项目一起版本管理。
