# 各方法对比：计量负荷

## 一、这次比的是什么

- 目标字段：`metered_load_mw`（计量负荷）
- 候选字段：46 个
- **统一预算 n = 7**，由本文方法的门控断崖自然切出来
- 全部 46 个字段的普通 DNN：R² = 0.9947（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9905** |
| Lasso | 线性稀疏（1996） | **0.9721** |
| XGBoost | 树模型特征重要性（2016） | **0.9668** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9545** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9531** |
| Pearson | 相关系数排序（只看两两关系） | **0.9412** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9905）。

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=7 | k=12 | k=19 | k=46 |
|---|---|---|---|---|---|---|
| DGatingDNN | 0.1420 | 0.9472 | 0.9905 | 0.9939 | 0.9946 | 0.9948 |
| Lasso | 0.3265 | 0.8747 | 0.9721 | 0.9850 | 0.9956 | 0.9950 |
| XGBoost | 0.5600 | 0.8933 | 0.9668 | 0.9790 | 0.9851 | 0.9948 |
| LassoNet | 0.5452 | 0.8397 | 0.9545 | 0.9827 | 0.9909 | 0.9948 |
| STG | 0.4700 | 0.7485 | 0.9531 | 0.9824 | 0.9835 | 0.9953 |
| Pearson | 0.5452 | 0.8397 | 0.9412 | 0.9711 | 0.9816 | 0.9947 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9934
- 活跃阈值 0.01，最终活跃字段 7 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9816
- 活跃阈值 0.5，最终活跃字段 10 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 441 个点，字段数从 0–46 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 2 / 7 | 29% |
| LassoNet | 1 / 7 | 14% |
| XGBoost | 0 / 7 | 0% |
| Lasso | 5 / 7 | 71% |
| Pearson | 0 / 7 | 0% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `gen_fuel_wind_pct` | 风电占比 |
| 2 | `gen_fuel_wind_mw` | 风电出力 |
| 3 | `gen_fuel_hydro_mw` | 水电出力 |
| 4 | `gen_fuel_hydro_pct` | 水电占比 |
| 5 | `gen_fuel_multiple_fuels_mw` | 多燃料出力 |
| 6 | `gen_fuel_multiple_fuels_pct` | 多燃料占比 |
| 7 | `net_actual_interchange_mw` | 净实际交换 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
