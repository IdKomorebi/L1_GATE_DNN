# 各方法对比：净实际交换功率

## 一、这次比的是什么

- 目标字段：`net_actual_interchange_mw`（净实际交换功率）
- 候选字段：55 个
- **统一预算 n = 17**，由本文方法的门控断崖自然切出来
- 全部 55 个字段的普通 DNN：R² = 0.9700（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9678** |
| Lasso | 线性稀疏（1996） | **0.9673** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9449** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9203** |
| XGBoost | 树模型特征重要性（2016） | **0.9055** |
| Pearson | 相关系数排序（只看两两关系） | **0.8750** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9678）。

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=8 | k=17 | k=22 | k=34 | k=55 |
|---|---|---|---|---|---|---|---|
| DGatingDNN | 0.0424 | 0.1952 | 0.9457 | 0.9678 | 0.9684 | 0.9713 | 0.9696 |
| Lasso | 0.0424 | 0.4600 | 0.9360 | 0.9673 | 0.9698 | 0.9710 | 0.9688 |
| LassoNet | 0.2300 | 0.5744 | 0.8512 | 0.9449 | 0.9721 | 0.9712 | 0.9730 |
| STG | 0.0155 | 0.2359 | 0.6435 | 0.9203 | 0.9249 | 0.9332 | 0.9692 |
| XGBoost | 0.2953 | 0.4432 | 0.8512 | 0.9055 | 0.9184 | 0.9693 | 0.9699 |
| Pearson | 0.2300 | 0.5390 | 0.7752 | 0.8750 | 0.8965 | 0.9631 | 0.9721 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9642
- 活跃阈值 0.01，最终活跃字段 17 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9346
- 活跃阈值 0.5，最终活跃字段 19 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 421 个点，字段数从 0–55 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 7 / 17 | 41% |
| LassoNet | 11 / 17 | 65% |
| XGBoost | 7 / 17 | 41% |
| Lasso | 14 / 17 | 82% |
| Pearson | 7 / 17 | 41% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `prelim_load_avg_hourly` | 预估小时负荷 |
| 2 | `gen_fuel_nuclear_pct` | 核电占比 |
| 3 | `metered_load_mw` | 计量负荷 |
| 4 | `gen_fuel_gas_mw` | 燃气出力 |
| 5 | `gen_fuel_nuclear_mw` | 核电出力 |
| 6 | `gen_fuel_coal_mw` | 燃煤出力 |
| 7 | `gen_fuel_solar_mw` | 光伏出力 |
| 8 | `gross_sched_interchange_mw` | 总计划交换 |
| 9 | `gen_fuel_hydro_mw` | 水电出力 |
| 10 | `total_losses` | 总网损 |
| 11 | `gen_fuel_wind_mw` | 风电出力 |
| 12 | `da_as_as_mw_primary_reserve` | 日前主用备用实际量 |
| 13 | `da_as_as_mw_synchronized_reserve` | 日前同步备用实际量 |
| 14 | `gross_inadv_interchange_mw` | 总非计划交换 |
| 15 | `total_pjm_reg_purchases` | 调频购买量 |
| 16 | `gen_fuel_oil_mw` | 燃油出力 |
| 17 | `gen_fuel_multiple_fuels_pct` | 多燃料占比 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
