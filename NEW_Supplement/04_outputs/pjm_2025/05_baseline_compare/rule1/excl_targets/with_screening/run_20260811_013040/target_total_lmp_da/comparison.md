# 各方法对比：日前总电价

## 一、这次比的是什么

- 目标字段：`total_lmp_da`（日前总电价）
- 候选字段：35 个
- **统一预算 n = 1**，由本文方法的门控断崖自然切出来
- 全部 35 个字段的普通 DNN：R² = 0.9943（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9865** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9865** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9865** |
| XGBoost | 树模型特征重要性（2016） | **0.9865** |
| Lasso | 线性稀疏（1996） | **0.9865** |
| Pearson | 相关系数排序（只看两两关系） | **0.9865** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9865）。

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=2 | k=3 | k=6 | k=13 | k=35 |
|---|---|---|---|---|---|---|
| DGatingDNN | 0.9865 | 0.9894 | 0.9905 | 0.9891 | 0.9918 | 0.9941 |
| STG | 0.9865 | 0.9894 | 0.9905 | 0.9891 | 0.9918 | 0.9941 |
| LassoNet | 0.9865 | 0.9889 | 0.9896 | 0.9910 | 0.9932 | 0.9943 |
| XGBoost | 0.9865 | 0.9869 | 0.9899 | 0.9926 | 0.9922 | 0.9941 |
| Lasso | 0.9865 | 0.9876 | 0.9892 | 0.9903 | 0.9930 | 0.9939 |
| Pearson | 0.9865 | 0.9876 | 0.9888 | 0.9884 | 0.9930 | 0.9935 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9885
- 活跃阈值 0.01，最终活跃字段 1 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9875
- 活跃阈值 0.5，最终活跃字段 1 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 461 个点，字段数从 0–35 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 1 / 1 | 100% |
| LassoNet | 1 / 1 | 100% |
| XGBoost | 1 / 1 | 100% |
| Lasso | 1 / 1 | 100% |
| Pearson | 1 / 1 | 100% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `system_energy_price_da` | 日前能量价格 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
