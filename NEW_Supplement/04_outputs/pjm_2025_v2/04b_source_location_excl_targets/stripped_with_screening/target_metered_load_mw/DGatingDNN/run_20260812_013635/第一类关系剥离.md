# 第一类关系剥离过程：计量负荷

候选池设定：排除其余关注字段

剥离分两层：**第一层按已知公式，不看残差**；第二层按数据发现的关系逐层剥离。

## 第一层：按已知公式剥离

该目标不在任何已知公式中，本层未剔除字段。

## 第二层：按数据发现的关系逐层剥离

## 一、这个目标一共有 6 层关系

按误差量级归档：

- 高精度近似：4 层
- 近似关系：2 层

档次的划分标准：精确公式（相对残差 < 1e-6）、含舍入的公式（1e-6 ~ 1e-3）、
高精度近似（1e-3 ~ 3e-2）、近似关系（3e-2 ~ 1e-1）。
相对残差 = 拟合剩下的误差 ÷ 该目标字段自身的波动幅度。

## 二、一层一层是怎么挖出来的

每一层的做法：用当时还剩下的全部字段拟合目标，再从全字段出发逐个删掉贡献最小的，缩到最小的一组；
然后只删掉这组里贡献最大的那**一个**字段，进入下一层看还能不能再找出关系。
只删一个而不是整组删掉，是为了让剩下的字段有机会自己凑出新路径，这样才挖得深。

### 第 1 层　[高精度近似]　相对残差 7.10e-03（相当于 R² = 0.9999）

```
metered_load_mw = +1.001*prelim_load_avg_hourly +114.1*da_as_as_req_mw_primary_reserve -171.2*da_as_as_req_mw_synchronized_reserve +139.6*da_as_as_mw_primary_reserve -139.6*da_as_as_mw_synchronized_reserve -139.6*da_as_nsr_mw_primary_reserve +1.041e+04
```

- 这条关系用到 6 个字段
- **删掉贡献最大的 `da_as_nsr_mw_primary_reserve`（非同步备用）**
- 删完之后候选池剩 48 个字段，目标仍能被线性回归推到 R² = **0.9999**

### 第 2 层　[高精度近似]　相对残差 7.11e-03（相当于 R² = 0.9999）

```
metered_load_mw = +1.001*prelim_load_avg_hourly +124.4*da_as_as_req_mw_primary_reserve -186.6*da_as_as_req_mw_synchronized_reserve +1.138e+04
```

- 这条关系用到 3 个字段
- **删掉贡献最大的 `da_as_as_req_mw_synchronized_reserve`（日前同步备用需求）**
- 删完之后候选池剩 47 个字段，目标仍能被线性回归推到 R² = **0.9999**

### 第 3 层　[高精度近似]　相对残差 7.14e-03（相当于 R² = 0.9999）

```
metered_load_mw = +1*prelim_load_avg_hourly -489
```

- 这条关系用到 1 个字段
- **删掉贡献最大的 `prelim_load_avg_hourly`（预估小时负荷）**
- 删完之后候选池剩 46 个字段，目标仍能被线性回归推到 R² = **0.9999**

### 第 4 层　[高精度近似]　相对残差 1.41e-02（相当于 R² = 0.9998）

```
metered_load_mw = +1.026*total_pjm_rt_load_mwh -442.1
```

- 这条关系用到 1 个字段
- **删掉贡献最大的 `total_pjm_rt_load_mwh`（实时负荷电量）**
- 删完之后候选池剩 45 个字段，目标仍能被线性回归推到 R² = **0.9949**

### 第 5 层　[近似关系]　相对残差 8.75e-02（相当于 R² = 0.9923）

```
metered_load_mw = +0.9835*forecast_load_mw_latest_available -463.4*system_energy_price_rt +467*total_lmp_rt +650.2
```

- 这条关系用到 3 个字段
- **删掉贡献最大的 `total_lmp_rt`（实时总电价）**
- 删完之后候选池剩 44 个字段，目标仍能被线性回归推到 R² = **0.9949**

### 第 6 层　[近似关系]　相对残差 8.85e-02（相当于 R² = 0.9922）

```
metered_load_mw = +0.9896*forecast_load_mw_latest_available +302.6
```

- 这条关系用到 1 个字段
- **删掉贡献最大的 `forecast_load_mw_latest_available`（最新负荷预测）**
- 删完之后候选池剩 43 个字段，目标仍能被线性回归推到 R² = **0.9876**

## 三、最终从候选池里剥掉了这些字段

| 层 | 字段 | 业务含义 | 剥离后线性 R² |
|---|---|---|---|
| 1 | `da_as_nsr_mw_primary_reserve` | 非同步备用 | 0.9999 |
| 2 | `da_as_as_req_mw_synchronized_reserve` | 日前同步备用需求 | 0.9999 |
| 3 | `prelim_load_avg_hourly` | 预估小时负荷 | 0.9999 |
| 4 | `total_pjm_rt_load_mwh` | 实时负荷电量 | 0.9949 |
| 5 | `total_lmp_rt` | 实时总电价 | 0.9949 |
| 6 | `forecast_load_mw_latest_available` | 最新负荷预测 | 0.9876 |

候选池：**49 → 31 个字段**（实际剥掉 18 个）。

## 四、为什么必须先做这一步

这些关系有公式可循，属于已知的、可以靠规则枚举禁止的风险，不是本文要找的隐性推断源。
如果不剥离就直接跑门控模型，模型一定优先选中这些字段——因为它们推断得最准——
真正没有公式可循的推断路径就被完全盖住了。

剥离之后目标仍能被推到 R² = 0.9876，**这部分才是隐性推断的部分**。
