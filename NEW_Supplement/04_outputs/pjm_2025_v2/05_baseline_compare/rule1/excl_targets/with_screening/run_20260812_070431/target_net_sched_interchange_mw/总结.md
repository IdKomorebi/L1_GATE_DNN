# 各方法对比：净计划交换功率

## 一、这次比的是什么

- 目标字段：`net_sched_interchange_mw`（净计划交换功率）
- 候选字段：43 个
- **统一预算 n = 10**，由本文方法的门控断崖自然切出来
- 全部 43 个字段的普通 DNN：R² = 0.9676（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9610** |
| Lasso | 线性稀疏（1996） | **0.9544** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9479** |
| XGBoost | 树模型特征重要性（2016） | **0.8338** |
| Pearson | 相关系数排序（只看两两关系） | **0.8284** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.8048** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9610）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 预估小时负荷、核电占比、核电出力、燃煤出力、总计划交换、燃气出力、水电出力、光伏出力、非同步备用、风电出力 |
| STG | 核电占比、实时负荷电量、核电出力、燃气占比、燃气出力、总计划交换、燃煤占比、多燃料占比、燃煤出力、日前30分钟备用需求 |
| LassoNet | 总计划交换、日前30分钟备用需求、调频分配量、多燃料占比、燃气出力、调频性能信用额、日前能量价格、光伏出力、储能出力、实时边际损耗价 |
| XGBoost | 日前30分钟备用需求、多燃料占比、总计划交换、日前主用备用需求、核电出力、日前能量价格、实时边际损耗价、调频购买量、调频分配量、燃煤占比 |
| Lasso | 预估小时负荷、燃气出力、燃煤出力、光伏出力、风电出力、水电出力、核电出力、燃油出力、非同步备用、总计划交换 |
| Pearson | 总计划交换、日前同步备用需求、日前主用备用需求、日前30分钟备用需求、核电出力、日前30分钟备用实际量、日前主用备用实际量、日前同步备用实际量、多燃料占比、燃气出力 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=5 | k=10 | k=15 | k=22 | k=43 |
|---|---|---|---|---|---|---|---|
| DGatingDNN | 0.0407 | 0.8809 | 0.9328 | 0.9610 | 0.9611 | 0.9636 | 0.9657 |
| Lasso | 0.0407 | 0.4519 | 0.7526 | 0.9544 | 0.9667 | 0.9659 | 0.9683 |
| STG | 0.0642 | 0.8667 | 0.8751 | 0.9479 | 0.9644 | 0.9641 | 0.9688 |
| XGBoost | 0.2923 | 0.5889 | 0.7594 | 0.8338 | 0.8850 | 0.9606 | 0.9682 |
| Pearson | 0.2278 | 0.5533 | 0.7054 | 0.8284 | 0.8585 | 0.8960 | 0.9663 |
| LassoNet | 0.2278 | 0.5759 | 0.7007 | 0.8048 | 0.8693 | 0.9649 | 0.9677 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9562
- 活跃阈值 0.01，最终活跃字段 10 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9580
- 活跃阈值 0.0，最终活跃字段 19 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 419 个点，字段数从 0–43 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 5 / 10 | 50% |
| LassoNet | 3 / 10 | 30% |
| XGBoost | 2 / 10 | 20% |
| Lasso | 8 / 10 | 80% |
| Pearson | 3 / 10 | 30% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `prelim_load_avg_hourly` | 预估小时负荷 |
| 2 | `gen_fuel_nuclear_pct` | 核电占比 |
| 3 | `gen_fuel_nuclear_mw` | 核电出力 |
| 4 | `gen_fuel_coal_mw` | 燃煤出力 |
| 5 | `gross_sched_interchange_mw` | 总计划交换 |
| 6 | `gen_fuel_gas_mw` | 燃气出力 |
| 7 | `gen_fuel_hydro_mw` | 水电出力 |
| 8 | `gen_fuel_solar_mw` | 光伏出力 |
| 9 | `da_as_nsr_mw_primary_reserve` | 非同步备用 |
| 10 | `wind_generation_mw` | 风电出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
