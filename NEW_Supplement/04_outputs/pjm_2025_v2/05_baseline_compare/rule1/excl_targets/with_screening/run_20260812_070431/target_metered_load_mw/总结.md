# 各方法对比：计量负荷

## 一、这次比的是什么

- 目标字段：`metered_load_mw`（计量负荷）
- 候选字段：31 个
- **统一预算 n = 3**，由本文方法的门控断崖自然切出来
- 全部 31 个字段的普通 DNN：R² = 0.9953（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9864** |
| XGBoost | 树模型特征重要性（2016） | **0.9864** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9862** |
| Lasso | 线性稀疏（1996） | **0.9500** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9497** |
| Pearson | 相关系数排序（只看两两关系） | **0.9497** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9864）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 核电占比、核电出力、日前能量价格 |
| STG | 核电占比、日前能量价格、核电出力 |
| LassoNet | 核电占比、燃气出力、燃煤出力 |
| XGBoost | 核电占比、核电出力、日前能量价格 |
| Lasso | 燃气出力、核电占比、燃煤出力 |
| Pearson | 核电占比、燃气出力、燃煤出力 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=2 | k=3 | k=8 | k=15 | k=31 |
|---|---|---|---|---|---|---|
| DGatingDNN | 0.8690 | 0.9768 | 0.9864 | 0.9906 | 0.9916 | 0.9952 |
| XGBoost | 0.8690 | 0.9768 | 0.9864 | 0.9918 | 0.9934 | 0.9952 |
| STG | 0.8690 | 0.8735 | 0.9862 | 0.9919 | 0.9937 | 0.9954 |
| Lasso | 0.8267 | 0.9244 | 0.9500 | 0.9901 | 0.9938 | 0.9951 |
| LassoNet | 0.8690 | 0.9244 | 0.9497 | 0.9925 | 0.9936 | 0.9953 |
| Pearson | 0.8690 | 0.9244 | 0.9497 | 0.9910 | 0.9939 | 0.9952 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9873
- 活跃阈值 0.01，最终活跃字段 3 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9920
- 活跃阈值 0.0，最终活跃字段 6 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 451 个点，字段数从 0–31 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 3 / 3 | 100% |
| LassoNet | 1 / 3 | 33% |
| XGBoost | 3 / 3 | 100% |
| Lasso | 1 / 3 | 33% |
| Pearson | 1 / 3 | 33% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `gen_fuel_nuclear_pct` | 核电占比 |
| 2 | `gen_fuel_nuclear_mw` | 核电出力 |
| 3 | `system_energy_price_da` | 日前能量价格 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
