# 各方法对比：日前主用备用总量

## 一、这次比的是什么

- 目标字段：`da_as_total_mw_primary_reserve`（日前主用备用总量）
- 候选字段：47 个
- **统一预算 n = 10**，由本文方法的门控断崖自然切出来
- 全部 47 个字段的普通 DNN：R² = 0.8588（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| Pearson ⭐ | 相关系数排序（只看两两关系） | **0.8716** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.8682** |
| Lasso | 线性稀疏（1996） | **0.8618** |
| XGBoost | 树模型特征重要性（2016） | **0.8603** |
| DGatingDNN | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.8544** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.8365** |

> 同预算下最好的是 **Pearson**（R² = 0.8716）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 日前同步备用实际量、日前30分钟备用实际量、日前同步备用需求、水电占比、燃煤出力、最新负荷预测、多燃料占比、风电出力、调频分配量、调频自调度量 |
| STG | 日前30分钟备用实际量、日前同步备用实际量、日前同步备用需求、日前30分钟备用需求、预估小时负荷、日前主用备用出清价、水电出力、水电占比、日前主用备用需求、调频自调度量 |
| LassoNet | 日前主用备用需求、日前同步备用需求、日前30分钟备用实际量、核电出力、日前30分钟备用需求、调频自调度量、日前同步备用实际量、水电占比、预估小时负荷、水电出力 |
| XGBoost | 日前主用备用需求、日前30分钟备用需求、日前主用备用出清价、日前同步备用实际量、日前30分钟备用实际量、核电出力、燃煤占比、水电占比、水电出力、实时负荷电量 |
| Lasso | 日前主用备用需求、日前30分钟备用实际量、日前同步备用实际量、日前30分钟备用需求、核电出力、调频自调度量、水电占比、预估小时负荷、风电出力、机会成本信用 |
| Pearson | 日前主用备用需求、日前同步备用需求、日前30分钟备用需求、日前30分钟备用实际量、核电出力、燃气出力、日前同步备用实际量、预估小时负荷、实时负荷电量、最新负荷预测 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=5 | k=10 | k=15 | k=22 | k=47 |
|---|---|---|---|---|---|---|---|
| Pearson | 0.4669 | 0.4643 | 0.7080 | 0.8716 | 0.8756 | 0.8552 | 0.8468 |
| LassoNet | 0.4669 | 0.6130 | 0.7074 | 0.8682 | 0.8656 | 0.8750 | 0.8516 |
| Lasso | 0.4669 | 0.8163 | 0.8584 | 0.8618 | 0.8475 | 0.8529 | 0.8476 |
| XGBoost | 0.4669 | 0.5569 | 0.8204 | 0.8603 | 0.8740 | 0.8716 | 0.8422 |
| DGatingDNN | 0.5798 | 0.8143 | 0.8317 | 0.8544 | 0.8479 | 0.8598 | 0.8495 |
| STG | 0.3396 | 0.8115 | 0.8404 | 0.8365 | 0.8478 | 0.8472 | 0.8475 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.8620
- 活跃阈值 0.01，最终活跃字段 10 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.8465
- 活跃阈值 0.0，最终活跃字段 10 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 426 个点，字段数从 0–47 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 5 / 10 | 50% |
| LassoNet | 5 / 10 | 50% |
| XGBoost | 3 / 10 | 30% |
| Lasso | 4 / 10 | 40% |
| Pearson | 4 / 10 | 40% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `da_as_as_mw_synchronized_reserve` | 日前同步备用实际量 |
| 2 | `da_as_as_mw_thirty_minutes_reserve` | 日前30分钟备用实际量 |
| 3 | `da_as_as_req_mw_synchronized_reserve` | 日前同步备用需求 |
| 4 | `gen_fuel_hydro_pct` | 水电占比 |
| 5 | `gen_fuel_coal_mw` | 燃煤出力 |
| 6 | `forecast_load_mw_latest_available` | 最新负荷预测 |
| 7 | `gen_fuel_multiple_fuels_pct` | 多燃料占比 |
| 8 | `gen_fuel_wind_mw` | 风电出力 |
| 9 | `total_pjm_assigned_reg` | 调频分配量 |
| 10 | `total_pjm_self_sched_reg` | 调频自调度量 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
