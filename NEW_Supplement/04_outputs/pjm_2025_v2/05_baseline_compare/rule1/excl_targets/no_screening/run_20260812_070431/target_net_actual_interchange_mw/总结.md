# 各方法对比：净实际交换功率

## 一、这次比的是什么

- 目标字段：`net_actual_interchange_mw`（净实际交换功率）
- 候选字段：48 个
- **统一预算 n = 11**，由本文方法的门控断崖自然切出来
- 全部 48 个字段的普通 DNN：R² = 0.9673（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9605** |
| Lasso | 线性稀疏（1996） | **0.9576** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9204** |
| XGBoost | 树模型特征重要性（2016） | **0.8600** |
| Pearson | 相关系数排序（只看两两关系） | **0.8335** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.8196** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9605）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 预估小时负荷、燃气出力、核电占比、燃煤出力、核电出力、光伏出力、水电占比、风电出力、总计划交换、非同步备用、燃油出力 |
| STG | 核电出力、核电占比、燃煤占比、光伏出力、最新负荷预测、多燃料占比、总计划交换、其他可再生出力、燃气占比、燃气出力、日前30分钟备用实际量 |
| LassoNet | 总计划交换、日前30分钟备用需求、调频分配量、多燃料占比、燃气出力、调频性能信用额、日前能量价格、光伏出力、储能出力、实时边际损耗价、日前主用备用自调度 |
| XGBoost | 日前30分钟备用需求、多燃料占比、日前主用备用需求、总计划交换、核电出力、日前能量价格、实时边际损耗价、调频购买量、调频分配量、燃煤占比、燃煤出力 |
| Lasso | 预估小时负荷、燃气出力、燃煤出力、光伏出力、风电出力、核电出力、水电出力、燃油出力、非同步备用、总计划交换、调频购买量 |
| Pearson | 总计划交换、日前同步备用需求、日前主用备用需求、日前30分钟备用需求、核电出力、日前30分钟备用实际量、日前主用备用实际量、日前同步备用实际量、多燃料占比、燃气出力、调频分配量 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=5 | k=11 | k=16 | k=23 | k=48 |
|---|---|---|---|---|---|---|---|
| DGatingDNN | 0.0424 | 0.4062 | 0.9176 | 0.9605 | 0.9696 | 0.9696 | 0.9684 |
| Lasso | 0.0424 | 0.4600 | 0.7482 | 0.9576 | 0.9680 | 0.9699 | 0.9672 |
| STG | 0.3276 | 0.5599 | 0.8009 | 0.9204 | 0.9351 | 0.9657 | 0.9669 |
| XGBoost | 0.2953 | 0.4432 | 0.7746 | 0.8600 | 0.8910 | 0.9333 | 0.9688 |
| Pearson | 0.2300 | 0.5390 | 0.7054 | 0.8335 | 0.8744 | 0.8864 | 0.9686 |
| LassoNet | 0.2300 | 0.5777 | 0.6995 | 0.8196 | 0.9062 | 0.9643 | 0.9673 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9574
- 活跃阈值 0.01，最终活跃字段 11 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9324
- 活跃阈值 0.0，最终活跃字段 18 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 422 个点，字段数从 0–48 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 5 / 11 | 45% |
| LassoNet | 3 / 11 | 27% |
| XGBoost | 3 / 11 | 27% |
| Lasso | 9 / 11 | 82% |
| Pearson | 3 / 11 | 27% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `prelim_load_avg_hourly` | 预估小时负荷 |
| 2 | `gen_fuel_gas_mw` | 燃气出力 |
| 3 | `gen_fuel_nuclear_pct` | 核电占比 |
| 4 | `gen_fuel_coal_mw` | 燃煤出力 |
| 5 | `gen_fuel_nuclear_mw` | 核电出力 |
| 6 | `gen_fuel_solar_mw` | 光伏出力 |
| 7 | `gen_fuel_hydro_pct` | 水电占比 |
| 8 | `gen_fuel_wind_mw` | 风电出力 |
| 9 | `gross_sched_interchange_mw` | 总计划交换 |
| 10 | `da_as_nsr_mw_primary_reserve` | 非同步备用 |
| 11 | `gen_fuel_oil_mw` | 燃油出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
