# 00 前置可推断性检测：普通神经网络

## 一、先给结论

本目录只回答‘现有 X/Y 定义是否具备继续做剥离、初筛和推断源定位的基本可行性’，
不在这里做门控选择，也不把一个目标字段偷放进另一个目标的输入池。

- 名义候选发布字段为 **27** 个，其中 3 个全年恒定，实际携带时变信息的输入为 **24** 个。
- 名义敏感类型目标为 **130** 个，其中 22 个全年恒定，进入神经网络评估的时变目标为 **108** 个。
- 108 个时变目标并不是 108 个相互独立的自由度：训练段中前 10/16/28 个主成分分别解释 90%/95%/99% 的标准化目标方差。
- 因此不能仅凭‘X 比 Y 少’判定不可推断；应看每个目标在严格时间测试集上的 R²。

## 二、统一实验设置

- 数据：`rts_gmlc_hourly_2020_acpf_base.csv`，8784 个小时。
- 时间划分：前 6148 行训练、中间 879 行验证、最后 1757 行测试；不随机打乱跨越时间边界。
- 普通 MLP：192-128-64 隐层，最多 240 轮，验证集早停，随机种子 [42, 43, 44]。
- 所有 X/Y 标准化参数只由训练段估计。主指标逐目标计算测试集 R²，同时保留 RMSE、MAE、相关系数；二值机组状态另报 balanced accuracy 和 ROC-AUC。
- Ridge 仅作线性参照，不参与最终风险判定。

## 三、各划分总体结果

| 划分 | X 名义/时变 | Y/可评估 | MLP 宏平均 R² | 中位 R² | R²≥0.9 | R²≥0.7 | Ridge 宏平均 R² |
|---|---:|---:|---:|---:|---:|---:|---:|
| `01_public27_joint` | 27/24 | 108/107 | 0.9151 | 0.9414 | 85 | 104 | 0.7130 |
| `02_public27_grouped/bus_electrical` | 27/24 | 36/36 | 0.9419 | 0.9595 | 31 | 36 | 0.7115 |
| `02_public27_grouped/branch` | 27/24 | 50/50 | 0.9400 | 0.9620 | 43 | 49 | 0.8061 |
| `02_public27_grouped/generator_continuous` | 27/24 | 17/16 | 0.8638 | 0.9190 | 11 | 14 | 0.4803 |
| `02_public27_grouped/generator_status` | 27/24 | 5/5 | 0.8683 | 0.8633 | 1 | 5 | 0.5368 |
| `03_public_plus_tier1_voltage` | 30/27 | 105/104 | 0.9441 | 0.9675 | 91 | 102 | 0.7456 |
| `04_public_plus_tier1_tier2_bus_operational` | 52/49 | 83/82 | 0.9726 | 0.9921 | 78 | 81 | 0.8939 |
| `05_public_plus_all_bus_diagnostic` | 63/60 | 72/71 | 0.9727 | 0.9927 | 68 | 70 | 0.9077 |

## 四、如何解释分组和增加输入

只改变 Y 的分组、不增加任何输入时，逐目标 R² 相比联合模型平均变化 **+0.0108**，中位变化 **+0.0033**。
如果这个变化很小，说明‘输出太多导致网络互相干扰’不是主要问题；如果明显上升，则后续应按节点、线路、连续机组量和机组状态分开建模。

逐级加 X 的三组并不是最终发布建议，而是敏感性诊断：

1. `03` 只加入时变节点电压幅值（T1，相对低敏感）。
2. `04` 再加入节点 P/Q 和平衡注入（T2，中等敏感），只预测 T3 高敏感目标。
3. `05` 加入全部时变节点量，包括相角，只作为‘已知节点状态时能否恢复线路/机组’的诊断上界，不能据此把相角建议为公开字段。

## 五、目标分级建议

分级同时区分两个维度，避免把‘敏感程度’和‘容易预测’混为一谈：

- T0：全年恒定，无时变信息，先从目标集合剥离。
- T1：节点电压幅值，相对低敏但仍是运行状态；只能作为扩展 X 的敏感性实验。
- T2：节点 P/Q 与平衡注入，中等敏感；不建议直接并入默认公开 X。
- T3：节点相角、线路潮流/负载率、具名机组出力与状态，作为论文主目标池。

在同一敏感等级内部，再按 public-27 模型的测试 R² 标注 strong/usable/weak/not-inferable。完整逐字段结果见 `target_recommendations.csv`；论文主实验可优先选择 T3 且跨种子 R² 稳定的目标，同时保留少量难推目标作为负对照。

## 六、建议用于正式流程的 12 个核心目标

为了控制后续逐目标剥离、初筛和门控实验的规模，建议主文先使用下面 12 个代表目标，其余时变目标放入补充材料。选择时优先保证物理对象、区域和机组类型覆盖，而不是只挑 R² 最高的字段；所有入选字段均为非恒定 T3 目标，且仅用 public-27 的分组普通 MLP 测试 R² 不低于 0.70。

| 类别 | 目标字段 | 选择理由 | 分组 MLP R² | 跨种子标准差 |
|---|---|---|---:|---:|
| bus_angle | `bus_115_va_deg` | 区域 1 高负荷代表节点相角 | 0.9534 | 0.0032 |
| bus_angle | `bus_215_va_deg` | 区域 2 高负荷代表节点相角 | 0.9843 | 0.0007 |
| bus_angle | `bus_315_va_deg` | 区域 3 高负荷代表节点相角 | 0.9629 | 0.0026 |
| interarea_loading | `branch_ab1_loading_pct` | 关键跨区支路 AB1 负载率 | 0.8826 | 0.0074 |
| interarea_loading | `branch_ab2_loading_pct` | 关键跨区支路 AB2 负载率 | 0.9871 | 0.0006 |
| interarea_loading | `branch_ab3_loading_pct` | 关键跨区支路 AB3 负载率 | 0.8555 | 0.0177 |
| interarea_loading | `branch_ca_1_loading_pct` | 关键跨区支路 CA-1 负载率 | 0.9806 | 0.0013 |
| interarea_loading | `branch_cb_1_loading_pct` | 关键跨区支路 CB-1 负载率 | 0.9815 | 0.0014 |
| generator | `gen_121_nuclear_1_pg_mw` | 区域 1 核电机组有功出力 | 0.9977 | 0.0007 |
| generator | `gen_218_cc_1_pg_mw` | 区域 2 联合循环机组有功出力 | 0.9150 | 0.0086 |
| generator | `gen_317_wind_1_pg_mw` | 区域 3 风电机组有功出力 | 0.9108 | 0.0028 |
| generator | `gen_321_cc_1_status` | 启停较均衡的联合循环机组状态 | 0.9683 | 0.0055 |

建议另保留以下难推/不可评估字段作为负对照或补充分析：

- `branch_b10_loading_pct`：分组 R²=0.6731。
- `gen_223_steam_3_pg_mw`：分组 R²=0.3405。
- `gen_218_cc_1_qg_mvar`：分组 R²=0.6341。
- `gen_216_steam_1_pg_mw`：测试段无方差。

这 12 个字段是后续计算规模和论文展示上的代表集，不意味着其余 T3 字段不敏感。

## 七、后续流程边界

本次结果只能决定目标池怎样组织。正式流程仍应对每个选定目标分别执行：目标字段剥离 → 是否初筛两条分支 → 普通 DNN 能力上限 → D-Gating 推断源定位。
`03/04/05` 中移入 X 的细粒度字段不会自动进入正式默认候选发布集合。
