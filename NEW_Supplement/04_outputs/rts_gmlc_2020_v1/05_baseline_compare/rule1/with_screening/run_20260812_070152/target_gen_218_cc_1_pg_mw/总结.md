# 各方法对比：机组218联合循环出力

## 一、这次比的是什么

- 目标字段：`gen_218_cc_1_pg_mw`（机组218联合循环出力）
- 候选字段：20 个
- **统一预算 n = 3**，由本文方法的门控断崖自然切出来
- 全部 20 个字段的普通 DNN：R² = 0.9608（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9302** |
| Lasso | 线性稀疏（1996） | **0.8468** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.8238** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.7888** |
| Pearson | 相关系数排序（只看两两关系） | **0.6388** |
| XGBoost | 树模型特征重要性（2016） | **0.6365** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9302）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 区域2日前负荷、光伏出力、水电出力 |
| STG | 光伏出力、区域2实时负荷、区域2日前负荷 |
| LassoNet | 燃煤出力、系统网损、光伏出力 |
| XGBoost | 区域2旋转备用、区域2日前负荷、燃煤出力 |
| Lasso | 燃煤出力、系统网损、水电出力 |
| Pearson | 燃煤出力、区域2旋转备用、区域2实时负荷 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=2 | k=3 | k=8 | k=15 | k=20 |
|---|---|---|---|---|---|---|
| DGatingDNN | 0.4487 | 0.8246 | 0.9302 | 0.9443 | 0.9580 | 0.9625 |
| Lasso | 0.5657 | 0.7270 | 0.8468 | 0.9494 | 0.9566 | 0.9610 |
| STG | 0.2065 | 0.8247 | 0.8238 | 0.9449 | 0.9602 | 0.9657 |
| LassoNet | 0.5657 | 0.7270 | 0.7888 | 0.9454 | 0.9536 | 0.9592 |
| Pearson | 0.5657 | 0.6438 | 0.6388 | 0.8276 | 0.8722 | 0.9610 |
| XGBoost | 0.4486 | 0.4483 | 0.6365 | 0.9458 | 0.9603 | 0.9580 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9261
- 活跃阈值 0.01，最终活跃字段 3 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9415
- 活跃阈值 0.0，最终活跃字段 7 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 436 个点，字段数从 0–20 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 2 / 3 | 67% |
| LassoNet | 1 / 3 | 33% |
| XGBoost | 1 / 3 | 33% |
| Lasso | 1 / 3 | 33% |
| Pearson | 0 / 3 | 0% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `area_2_load_day_ahead_mw` | 区域2日前负荷 |
| 2 | `gen_fuel_solar_mw` | 光伏出力 |
| 3 | `gen_fuel_hydro_mw` | 水电出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
