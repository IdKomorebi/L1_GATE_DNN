# 各方法对比：线路CB-1负载率

## 一、这次比的是什么

- 目标字段：`branch_cb_1_loading_pct`（线路CB-1负载率）
- 候选字段：20 个
- **统一预算 n = 8**，由本文方法的门控断崖自然切出来
- 全部 20 个字段的普通 DNN：R² = 0.9568（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| Lasso ⭐ | 线性稀疏（1996） | **0.9540** |
| DGatingDNN | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9534** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9511** |
| XGBoost | 树模型特征重要性（2016） | **0.9393** |
| Pearson | 相关系数排序（只看两两关系） | **0.9151** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9060** |

> 同预算下最好的是 **Lasso**（R² = 0.9540）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 燃煤出力、燃气出力、风电出力、光伏出力、系统网损、区域1旋转备用、区域3旋转备用、区域2旋转备用 |
| STG | 燃煤出力、燃气出力、光伏出力、区域3日前负荷、系统网损、风电出力、区域1旋转备用、调频上备用 |
| LassoNet | 风电出力、系统网损、光伏出力、调频下备用、调频上备用、燃煤出力、区域3旋转备用、区域3实时负荷 |
| XGBoost | 风电出力、系统网损、燃气出力、光伏出力、燃煤出力、区域1日前负荷、核电出力、区域1旋转备用 |
| Lasso | 燃煤出力、光伏出力、区域1实时负荷、风电出力、系统网损、燃气出力、区域2实时负荷、区域3旋转备用 |
| Pearson | 风电出力、系统网损、光伏出力、调频上备用、调频下备用、核电出力、燃煤出力、区域3实时负荷 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=4 | k=8 | k=13 | k=20 |
|---|---|---|---|---|---|---|
| Lasso | 0.1235 | 0.8457 | 0.8789 | 0.9540 | 0.9567 | 0.9565 |
| DGatingDNN | 0.1235 | 0.8046 | 0.8410 | 0.9534 | 0.9526 | 0.9580 |
| STG | 0.1235 | 0.5465 | 0.6919 | 0.9511 | 0.9581 | 0.9577 |
| XGBoost | 0.5129 | 0.7972 | 0.8241 | 0.9393 | 0.9569 | 0.9561 |
| Pearson | 0.5129 | 0.7848 | 0.8038 | 0.9151 | 0.9523 | 0.9565 |
| LassoNet | 0.5129 | 0.7848 | 0.7991 | 0.9060 | 0.9532 | 0.9573 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9452
- 活跃阈值 0.01，最终活跃字段 8 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9522
- 活跃阈值 0.0，最终活跃字段 10 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 433 个点，字段数从 0–20 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 6 / 8 | 75% |
| LassoNet | 5 / 8 | 62% |
| XGBoost | 6 / 8 | 75% |
| Lasso | 6 / 8 | 75% |
| Pearson | 4 / 8 | 50% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `gen_fuel_coal_mw` | 燃煤出力 |
| 2 | `gen_fuel_natural_gas_mw` | 燃气出力 |
| 3 | `gen_fuel_wind_mw` | 风电出力 |
| 4 | `gen_fuel_solar_mw` | 光伏出力 |
| 5 | `system_losses_mw` | 系统网损 |
| 6 | `reserve_spin_r1_mw` | 区域1旋转备用 |
| 7 | `reserve_spin_r3_mw` | 区域3旋转备用 |
| 8 | `reserve_spin_r2_mw` | 区域2旋转备用 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
