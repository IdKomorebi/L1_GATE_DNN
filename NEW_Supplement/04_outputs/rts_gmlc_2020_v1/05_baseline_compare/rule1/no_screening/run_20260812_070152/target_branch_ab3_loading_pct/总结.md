# 各方法对比：线路AB3负载率

## 一、这次比的是什么

- 目标字段：`branch_ab3_loading_pct`（线路AB3负载率）
- 候选字段：21 个
- **统一预算 n = 9**，由本文方法的门控断崖自然切出来
- 全部 21 个字段的普通 DNN：R² = 0.9191（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| STG ⭐ | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9222** |
| DGatingDNN | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9218** |
| XGBoost | 树模型特征重要性（2016） | **0.9112** |
| Lasso | 线性稀疏（1996） | **0.8953** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.8611** |
| Pearson | 相关系数排序（只看两两关系） | **0.6485** |

> 同预算下最好的是 **STG**（R² = 0.9222）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 燃煤出力、燃气出力、区域2实时负荷、区域1实时负荷、风电出力、系统网损、水电出力、区域3旋转备用、光伏出力 |
| STG | 燃气出力、燃煤出力、风电出力、光伏出力、系统网损、水电出力、区域2实时负荷、区域1实时负荷、调频下备用 |
| LassoNet | 区域1旋转备用、系统网损、燃煤出力、光伏出力、风电出力、调频上备用、核电出力、燃气出力、调频下备用 |
| XGBoost | 区域1日前负荷、区域1实时负荷、区域1旋转备用、系统网损、风电出力、光伏出力、燃气出力、燃煤出力、区域2旋转备用 |
| Lasso | 区域1实时负荷、系统网损、燃煤出力、区域1日前负荷、水电出力、核电出力、燃气出力、区域3旋转备用、区域2旋转备用 |
| Pearson | 区域1实时负荷、区域1旋转备用、区域1日前负荷、系统发电量、区域2旋转备用、区域2实时负荷、区域2日前负荷、系统日前负荷、系统实时负荷 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=4 | k=9 | k=14 | k=21 |
|---|---|---|---|---|---|---|
| STG | 0.1319 | 0.7971 | 0.8246 | 0.9222 | 0.9217 | 0.9204 |
| DGatingDNN | 0.3295 | 0.7170 | 0.7754 | 0.9218 | 0.9211 | 0.9212 |
| XGBoost | 0.3960 | 0.3951 | 0.6222 | 0.9112 | 0.9187 | 0.9240 |
| Lasso | 0.3958 | 0.7258 | 0.7300 | 0.8953 | 0.9098 | 0.9213 |
| LassoNet | 0.3957 | 0.7301 | 0.8006 | 0.8611 | 0.9204 | 0.9199 |
| Pearson | 0.3958 | 0.3953 | 0.4460 | 0.6485 | 0.8242 | 0.9226 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9155
- 活跃阈值 0.01，最终活跃字段 9 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9236
- 活跃阈值 0.0，最终活跃字段 12 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 414 个点，字段数从 0–21 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 8 / 9 | 89% |
| LassoNet | 5 / 9 | 56% |
| XGBoost | 6 / 9 | 67% |
| Lasso | 6 / 9 | 67% |
| Pearson | 2 / 9 | 22% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `gen_fuel_coal_mw` | 燃煤出力 |
| 2 | `gen_fuel_natural_gas_mw` | 燃气出力 |
| 3 | `area_2_load_actual_mw` | 区域2实时负荷 |
| 4 | `area_1_load_actual_mw` | 区域1实时负荷 |
| 5 | `gen_fuel_wind_mw` | 风电出力 |
| 6 | `system_losses_mw` | 系统网损 |
| 7 | `gen_fuel_hydro_mw` | 水电出力 |
| 8 | `reserve_spin_r3_mw` | 区域3旋转备用 |
| 9 | `gen_fuel_solar_mw` | 光伏出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
