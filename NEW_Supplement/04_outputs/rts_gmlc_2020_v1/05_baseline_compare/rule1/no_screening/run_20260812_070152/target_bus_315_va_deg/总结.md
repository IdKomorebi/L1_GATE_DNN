# 各方法对比：节点315电压相角

## 一、这次比的是什么

- 目标字段：`bus_315_va_deg`（节点315电压相角）
- 候选字段：24 个
- **统一预算 n = 11**，由本文方法的门控断崖自然切出来
- 全部 24 个字段的普通 DNN：R² = 0.9719（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9695** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9687** |
| XGBoost | 树模型特征重要性（2016） | **0.9656** |
| Lasso | 线性稀疏（1996） | **0.9655** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9588** |
| Pearson | 相关系数排序（只看两两关系） | **0.9578** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9695）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 系统网损、燃气出力、风电出力、区域1日前负荷、区域2实时负荷、光伏出力、区域2净送出、区域3日前负荷、区域3净送出、水电出力、核电出力 |
| STG | 燃气出力、系统网损、风电出力、燃煤出力、光伏出力、水电出力、区域3净送出、核电出力、调频上备用、区域2净送出、区域1净送出 |
| LassoNet | 风电出力、光伏出力、区域1净送出、系统网损、区域3净送出、核电出力、水电出力、燃煤出力、区域2日前负荷、区域3旋转备用、区域3实时负荷 |
| XGBoost | 风电出力、光伏出力、系统网损、区域3净送出、核电出力、燃煤出力、区域1净送出、区域1日前负荷、燃气出力、调频上备用、区域3日前负荷 |
| Lasso | 区域3净送出、系统网损、光伏出力、区域1净送出、风电出力、区域2净送出、区域2旋转备用、水电出力、燃煤出力、区域1实时负荷、核电出力 |
| Pearson | 风电出力、区域2净送出、光伏出力、区域1净送出、系统网损、水电出力、核电出力、燃煤出力、区域3日前负荷、区域3实时负荷、区域3旋转备用 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=5 | k=11 | k=16 | k=23 | k=24 |
|---|---|---|---|---|---|---|---|
| DGatingDNN | 0.2458 | 0.8495 | 0.9003 | 0.9695 | 0.9718 | 0.9724 | 0.9736 |
| STG | 0.1147 | 0.8504 | 0.9083 | 0.9687 | 0.9735 | 0.9729 | 0.9737 |
| XGBoost | 0.6767 | 0.8572 | 0.9097 | 0.9656 | 0.9728 | 0.9740 | 0.9735 |
| Lasso | 0.1481 | 0.7658 | 0.9086 | 0.9655 | 0.9721 | 0.9737 | 0.9730 |
| LassoNet | 0.6767 | 0.8423 | 0.9080 | 0.9588 | 0.9692 | 0.9718 | 0.9725 |
| Pearson | 0.6767 | 0.8252 | 0.9074 | 0.9578 | 0.9725 | 0.9723 | 0.9719 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9595
- 活跃阈值 0.01，最终活跃字段 11 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9570
- 活跃阈值 0.0，最终活跃字段 10 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 440 个点，字段数从 0–24 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 8 / 11 | 73% |
| LassoNet | 6 / 11 | 55% |
| XGBoost | 8 / 11 | 73% |
| Lasso | 7 / 11 | 64% |
| Pearson | 7 / 11 | 64% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `system_losses_mw` | 系统网损 |
| 2 | `gen_fuel_natural_gas_mw` | 燃气出力 |
| 3 | `gen_fuel_wind_mw` | 风电出力 |
| 4 | `area_1_load_day_ahead_mw` | 区域1日前负荷 |
| 5 | `area_2_load_actual_mw` | 区域2实时负荷 |
| 6 | `gen_fuel_solar_mw` | 光伏出力 |
| 7 | `area_2_net_export_mw` | 区域2净送出 |
| 8 | `area_3_load_day_ahead_mw` | 区域3日前负荷 |
| 9 | `area_3_net_export_mw` | 区域3净送出 |
| 10 | `gen_fuel_hydro_mw` | 水电出力 |
| 11 | `gen_fuel_nuclear_mw` | 核电出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
