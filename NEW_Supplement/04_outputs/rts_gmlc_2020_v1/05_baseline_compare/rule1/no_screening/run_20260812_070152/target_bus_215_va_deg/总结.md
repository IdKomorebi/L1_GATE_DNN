# 各方法对比：节点215电压相角

## 一、这次比的是什么

- 目标字段：`bus_215_va_deg`（节点215电压相角）
- 候选字段：24 个
- **统一预算 n = 8**，由本文方法的门控断崖自然切出来
- 全部 24 个字段的普通 DNN：R² = 0.9883（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| Lasso ⭐ | 线性稀疏（1996） | **0.9828** |
| DGatingDNN | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9769** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9737** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9720** |
| XGBoost | 树模型特征重要性（2016） | **0.9664** |
| Pearson | 相关系数排序（只看两两关系） | **0.7219** |

> 同预算下最好的是 **Lasso**（R² = 0.9828）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 燃气出力、区域1日前负荷、燃煤出力、光伏出力、风电出力、系统网损、区域3净送出、水电出力 |
| STG | 区域1实时负荷、燃气出力、风电出力、系统网损、光伏出力、区域3净送出、区域1旋转备用、水电出力 |
| LassoNet | 系统网损、光伏出力、区域3净送出、区域1实时负荷、风电出力、水电出力、区域2净送出、燃气出力 |
| XGBoost | 系统网损、光伏出力、区域1日前负荷、区域3净送出、燃煤出力、区域1实时负荷、水电出力、调频上备用 |
| Lasso | 区域1净送出、区域2净送出、区域3净送出、风电出力、燃气出力、区域1实时负荷、水电出力、光伏出力 |
| Pearson | 系统网损、区域2旋转备用、区域2实时负荷、区域2日前负荷、风电出力、区域1旋转备用、区域1实时负荷、区域1日前负荷 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=4 | k=8 | k=13 | k=20 | k=24 |
|---|---|---|---|---|---|---|---|
| Lasso | 0.1803 | 0.8749 | 0.9077 | 0.9828 | 0.9868 | 0.9883 | 0.9880 |
| DGatingDNN | 0.2462 | 0.5886 | 0.9293 | 0.9769 | 0.9822 | 0.9834 | 0.9880 |
| LassoNet | 0.6020 | 0.8357 | 0.9189 | 0.9737 | 0.9836 | 0.9885 | 0.9888 |
| STG | 0.2442 | 0.8687 | 0.8908 | 0.9720 | 0.9802 | 0.9887 | 0.9887 |
| XGBoost | 0.6020 | 0.8504 | 0.9175 | 0.9664 | 0.9793 | 0.9827 | 0.9903 |
| Pearson | 0.6020 | 0.6447 | 0.6453 | 0.7219 | 0.9364 | 0.9818 | 0.9886 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9739
- 活跃阈值 0.01，最终活跃字段 8 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9783
- 活跃阈值 0.0，最终活跃字段 10 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 443 个点，字段数从 0–24 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 6 / 8 | 75% |
| LassoNet | 6 / 8 | 75% |
| XGBoost | 6 / 8 | 75% |
| Lasso | 5 / 8 | 62% |
| Pearson | 3 / 8 | 38% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `gen_fuel_natural_gas_mw` | 燃气出力 |
| 2 | `area_1_load_day_ahead_mw` | 区域1日前负荷 |
| 3 | `gen_fuel_coal_mw` | 燃煤出力 |
| 4 | `gen_fuel_solar_mw` | 光伏出力 |
| 5 | `gen_fuel_wind_mw` | 风电出力 |
| 6 | `system_losses_mw` | 系统网损 |
| 7 | `area_3_net_export_mw` | 区域3净送出 |
| 8 | `gen_fuel_hydro_mw` | 水电出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
