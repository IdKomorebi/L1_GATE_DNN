# 各方法对比：机组321投入状态

## 一、这次比的是什么

- 目标字段：`gen_321_cc_1_status`（机组321投入状态）
- 候选字段：22 个
- **统一预算 n = 11**，由本文方法的门控断崖自然切出来
- 全部 22 个字段的普通 DNN：R² = 0.9707（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9606** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9283** |
| XGBoost | 树模型特征重要性（2016） | **0.9277** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9260** |
| Lasso | 线性稀疏（1996） | **0.9168** |
| Pearson | 相关系数排序（只看两两关系） | **0.8528** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9606）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 光伏出力、风电出力、燃煤出力、区域3实时负荷、区域1日前负荷、水电出力、区域3净送出、系统网损、核电出力、区域3旋转备用、调频下备用 |
| STG | 光伏出力、风电出力、区域3旋转备用、系统网损、区域2净送出、区域3净送出、燃煤出力、调频上备用、区域3日前负荷、区域3实时负荷、调频下备用 |
| LassoNet | 风电出力、光伏出力、区域3日前负荷、区域3实时负荷、区域3净送出、区域3旋转备用、区域1日前负荷、核电出力、燃煤出力、调频下备用、区域1旋转备用 |
| XGBoost | 燃煤出力、光伏出力、风电出力、区域3旋转备用、区域3日前负荷、区域3实时负荷、调频下备用、区域2日前负荷、区域3净送出、核电出力、系统网损 |
| Lasso | 光伏出力、风电出力、区域3实时负荷、区域3净送出、区域2净送出、核电出力、调频下备用、区域3日前负荷、水电出力、燃煤出力、区域3旋转备用 |
| Pearson | 燃煤出力、风电出力、光伏出力、核电出力、区域2净送出、区域1净送出、调频上备用、调频下备用、区域2日前负荷、区域2实时负荷、区域2旋转备用 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=5 | k=11 | k=16 | k=22 |
|---|---|---|---|---|---|---|
| DGatingDNN | 0.1551 | 0.7519 | 0.9107 | 0.9606 | 0.9682 | 0.9732 |
| STG | 0.1551 | 0.8743 | 0.8881 | 0.9283 | 0.9677 | 0.9691 |
| XGBoost | 0.4144 | 0.7507 | 0.8985 | 0.9277 | 0.9657 | 0.9651 |
| LassoNet | 0.2787 | 0.8742 | 0.9078 | 0.9260 | 0.9336 | 0.9689 |
| Lasso | 0.1551 | 0.8743 | 0.9070 | 0.9168 | 0.9682 | 0.9704 |
| Pearson | 0.4144 | 0.7501 | 0.7624 | 0.8528 | 0.9245 | 0.9699 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9572
- 活跃阈值 0.01，最终活跃字段 11 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9332
- 活跃阈值 0.0，最终活跃字段 10 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 424 个点，字段数从 0–22 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 8 / 11 | 73% |
| LassoNet | 9 / 11 | 82% |
| XGBoost | 9 / 11 | 82% |
| Lasso | 9 / 11 | 82% |
| Pearson | 5 / 11 | 45% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `gen_fuel_solar_mw` | 光伏出力 |
| 2 | `gen_fuel_wind_mw` | 风电出力 |
| 3 | `gen_fuel_coal_mw` | 燃煤出力 |
| 4 | `area_3_load_actual_mw` | 区域3实时负荷 |
| 5 | `area_1_load_day_ahead_mw` | 区域1日前负荷 |
| 6 | `gen_fuel_hydro_mw` | 水电出力 |
| 7 | `area_3_net_export_mw` | 区域3净送出 |
| 8 | `system_losses_mw` | 系统网损 |
| 9 | `gen_fuel_nuclear_mw` | 核电出力 |
| 10 | `reserve_spin_r3_mw` | 区域3旋转备用 |
| 11 | `reserve_reg_down_mw` | 调频下备用 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
