# 各方法对比：机组317风电出力

## 一、这次比的是什么

- 目标字段：`gen_317_wind_1_pg_mw`（机组317风电出力）
- 候选字段：40 个
- **统一预算 n = 11**，由本文方法的门控断崖自然切出来
- 全部 40 个字段的普通 DNN：R² = 0.9554（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9461** |
| Lasso | 线性稀疏（1996） | **0.9433** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.8921** |
| XGBoost | 树模型特征重要性（2016） | **0.8733** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.8711** |
| Pearson | 相关系数排序（只看两两关系） | **0.8562** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9461）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 区域1净送出、区域3净送出、区域2净送出、区域3燃气出力、区域3光伏出力、区域3旋转备用、小时、区域1风电出力、系统网损、调频下备用、区域3水电出力 |
| STG | 区域3净送出、系统网损、区域1风电出力、区域3光伏出力、调频下备用、区域3燃气出力、光伏出力、区域2净送出、区域1光伏出力、调频上备用、区域3燃煤出力 |
| LassoNet | 区域1风电出力、区域3燃气出力、区域3光伏出力、光伏出力、区域3燃煤出力、调频下备用、调频上备用、区域1光伏出力、区域2水电出力、区域3实时负荷、区域3日前负荷 |
| XGBoost | 区域1风电出力、区域3燃煤出力、区域3光伏出力、区域1光伏出力、区域3燃气出力、小时、光伏出力、区域2日前负荷、区域2水电出力、调频下备用、区域2发电总量 |
| Lasso | 区域1净送出、区域3净送出、区域2净送出、区域3光伏出力、区域3燃气出力、区域3实时负荷、区域1风电出力、区域3燃煤出力、系统网损、区域3旋转备用、区域1光伏出力 |
| Pearson | 区域1风电出力、区域2净送出、区域1净送出、区域1燃煤出力、区域3燃气出力、区域3燃煤出力、燃煤出力、核电出力、区域1核电出力、燃气出力、系统网损 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=5 | k=11 | k=16 | k=23 | k=40 |
|---|---|---|---|---|---|---|---|
| DGatingDNN | 0.3991 | 0.6585 | 0.8829 | 0.9461 | 0.9526 | 0.9602 | 0.9529 |
| Lasso | 0.3991 | 0.6585 | 0.8825 | 0.9433 | 0.9525 | 0.9592 | 0.9559 |
| STG | 0.2156 | 0.7313 | 0.8032 | 0.8921 | 0.9208 | 0.9305 | 0.9608 |
| XGBoost | 0.6939 | 0.7948 | 0.8134 | 0.8733 | 0.9124 | 0.9320 | 0.9522 |
| LassoNet | 0.6939 | 0.8112 | 0.8138 | 0.8711 | 0.9160 | 0.9252 | 0.9546 |
| Pearson | 0.6939 | 0.7194 | 0.8121 | 0.8562 | 0.9089 | 0.9216 | 0.9563 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9427
- 活跃阈值 0.01，最终活跃字段 11 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.8967
- 活跃阈值 0.0，最终活跃字段 11 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 442 个点，字段数从 0–40 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 7 / 11 | 64% |
| LassoNet | 4 / 11 | 36% |
| XGBoost | 5 / 11 | 45% |
| Lasso | 8 / 11 | 73% |
| Pearson | 5 / 11 | 45% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `area_1_net_export_mw` | 区域1净送出 |
| 2 | `area_3_net_export_mw` | 区域3净送出 |
| 3 | `area_2_net_export_mw` | 区域2净送出 |
| 4 | `area_3_fuel_natural_gas_mw` | 区域3燃气出力 |
| 5 | `area_3_fuel_solar_mw` | 区域3光伏出力 |
| 6 | `reserve_spin_r3_mw` | 区域3旋转备用 |
| 7 | `hour_of_day` | 小时 |
| 8 | `area_1_fuel_wind_mw` | 区域1风电出力 |
| 9 | `system_losses_mw` | 系统网损 |
| 10 | `reserve_reg_down_mw` | 调频下备用 |
| 11 | `area_3_fuel_hydro_mw` | 区域3水电出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
