# 各方法对比：节点215电压相角

## 一、这次比的是什么

- 目标字段：`bus_215_va_deg`（节点215电压相角）
- 候选字段：34 个
- **统一预算 n = 9**，由本文方法的门控断崖自然切出来
- 全部 34 个字段的普通 DNN：R² = 0.9871（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9714** |
| Lasso | 线性稀疏（1996） | **0.9710** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9641** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9468** |
| XGBoost | 树模型特征重要性（2016） | **0.9399** |
| Pearson | 相关系数排序（只看两两关系） | **0.8432** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9714）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 区域1燃煤出力、区域1日前负荷、风电出力、区域1光伏出力、光伏出力、区域2发电总量、区域3燃煤出力、区域2燃煤出力、区域1净送出 |
| STG | 光伏出力、区域1燃煤出力、区域1日前负荷、区域1实时负荷、区域2燃煤出力、区域1光伏出力、系统网损、区域1净送出、区域1风电出力 |
| LassoNet | 系统网损、区域3光伏出力、区域1净送出、区域1燃气出力、区域1风电出力、区域1实时负荷、区域1光伏出力、区域1旋转备用、调频下备用 |
| XGBoost | 系统网损、区域3光伏出力、区域1光伏出力、区域1日前负荷、区域1风电出力、区域1发电总量、调频上备用、区域3燃煤出力、区域1实时负荷 |
| Lasso | 区域1实时负荷、光伏出力、区域2发电总量、区域1燃煤出力、区域2燃煤出力、区域1光伏出力、区域1净送出、风电出力、区域1风电出力 |
| Pearson | 系统网损、区域2燃气出力、区域2燃煤出力、区域2发电总量、区域2旋转备用、区域2实时负荷、区域2日前负荷、区域1风电出力、风电出力 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=4 | k=9 | k=14 | k=21 | k=34 |
|---|---|---|---|---|---|---|---|
| DGatingDNN | 0.3277 | 0.7980 | 0.9379 | 0.9714 | 0.9735 | 0.9841 | 0.9871 |
| Lasso | 0.2442 | 0.6473 | 0.9354 | 0.9710 | 0.9738 | 0.9864 | 0.9865 |
| STG | 0.1936 | 0.9293 | 0.9299 | 0.9641 | 0.9724 | 0.9840 | 0.9875 |
| LassoNet | 0.6020 | 0.8031 | 0.8587 | 0.9468 | 0.9704 | 0.9781 | 0.9871 |
| XGBoost | 0.6020 | 0.7822 | 0.8638 | 0.9399 | 0.9670 | 0.9748 | 0.9873 |
| Pearson | 0.6020 | 0.6788 | 0.7748 | 0.8432 | 0.9624 | 0.9804 | 0.9875 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9665
- 活跃阈值 0.01，最终活跃字段 9 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9687
- 活跃阈值 0.0，最终活跃字段 12 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 441 个点，字段数从 0–34 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 6 / 9 | 67% |
| LassoNet | 2 / 9 | 22% |
| XGBoost | 3 / 9 | 33% |
| Lasso | 7 / 9 | 78% |
| Pearson | 3 / 9 | 33% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `area_1_fuel_coal_mw` | 区域1燃煤出力 |
| 2 | `area_1_load_day_ahead_mw` | 区域1日前负荷 |
| 3 | `gen_fuel_wind_mw` | 风电出力 |
| 4 | `area_1_fuel_solar_mw` | 区域1光伏出力 |
| 5 | `gen_fuel_solar_mw` | 光伏出力 |
| 6 | `area_2_generation_mw` | 区域2发电总量 |
| 7 | `area_3_fuel_coal_mw` | 区域3燃煤出力 |
| 8 | `area_2_fuel_coal_mw` | 区域2燃煤出力 |
| 9 | `area_1_net_export_mw` | 区域1净送出 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
