# 各方法对比：线路AB2负载率

## 一、这次比的是什么

- 目标字段：`branch_ab2_loading_pct`（线路AB2负载率）
- 候选字段：29 个
- **统一预算 n = 8**，由本文方法的门控断崖自然切出来
- 全部 29 个字段的普通 DNN：R² = 0.9842（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9730** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9666** |
| Lasso | 线性稀疏（1996） | **0.9596** |
| XGBoost | 树模型特征重要性（2016） | **0.9541** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9466** |
| Pearson | 相关系数排序（只看两两关系） | **0.8372** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9730）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 燃气出力、区域1燃煤出力、区域1燃气出力、区域1日前负荷、区域1光伏出力、区域2实时负荷、区域3光伏出力、区域2燃煤出力 |
| STG | 系统网损、区域1燃煤出力、燃气出力、区域1日前负荷、区域1光伏出力、区域1燃气出力、区域3光伏出力、区域1风电出力 |
| LassoNet | 系统网损、区域3光伏出力、区域1燃气出力、区域1风电出力、区域1旋转备用、区域1光伏出力、区域1日前负荷、小时 |
| XGBoost | 系统网损、区域3光伏出力、区域1光伏出力、区域1日前负荷、区域1风电出力、区域1发电总量、小时、区域1燃煤出力 |
| Lasso | 区域1实时负荷、区域1发电总量、区域1燃煤出力、光伏出力、区域2发电总量、区域1光伏出力、区域2燃煤出力、区域1风电出力 |
| Pearson | 系统网损、区域2燃气出力、区域2燃煤出力、区域2发电总量、区域2旋转备用、区域2实时负荷、区域2日前负荷、区域1发电总量 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=4 | k=8 | k=13 | k=20 | k=29 |
|---|---|---|---|---|---|---|---|
| DGatingDNN | 0.2621 | 0.5674 | 0.7834 | 0.9730 | 0.9828 | 0.9854 | 0.9844 |
| STG | 0.6230 | 0.8073 | 0.8641 | 0.9666 | 0.9768 | 0.9812 | 0.9841 |
| Lasso | 0.2520 | 0.6419 | 0.9348 | 0.9596 | 0.9798 | 0.9835 | 0.9843 |
| XGBoost | 0.6230 | 0.7809 | 0.8788 | 0.9541 | 0.9665 | 0.9840 | 0.9848 |
| LassoNet | 0.6230 | 0.8394 | 0.8668 | 0.9466 | 0.9639 | 0.9687 | 0.9849 |
| Pearson | 0.6230 | 0.6959 | 0.7812 | 0.8372 | 0.8929 | 0.9788 | 0.9854 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9697
- 活跃阈值 0.01，最终活跃字段 8 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9773
- 活跃阈值 0.0，最终活跃字段 14 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 442 个点，字段数从 0–29 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 6 / 8 | 75% |
| LassoNet | 4 / 8 | 50% |
| XGBoost | 4 / 8 | 50% |
| Lasso | 3 / 8 | 38% |
| Pearson | 2 / 8 | 25% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `gen_fuel_natural_gas_mw` | 燃气出力 |
| 2 | `area_1_fuel_coal_mw` | 区域1燃煤出力 |
| 3 | `area_1_fuel_natural_gas_mw` | 区域1燃气出力 |
| 4 | `area_1_load_day_ahead_mw` | 区域1日前负荷 |
| 5 | `area_1_fuel_solar_mw` | 区域1光伏出力 |
| 6 | `area_2_load_actual_mw` | 区域2实时负荷 |
| 7 | `area_3_fuel_solar_mw` | 区域3光伏出力 |
| 8 | `area_2_fuel_coal_mw` | 区域2燃煤出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
