# 各方法对比：线路CA-1负载率

## 一、这次比的是什么

- 目标字段：`branch_ca_1_loading_pct`（线路CA-1负载率）
- 候选字段：43 个
- **统一预算 n = 11**，由本文方法的门控断崖自然切出来
- 全部 43 个字段的普通 DNN：R² = 0.9775（能力上限参照）

每个方法按自己的排序取前 n 个字段，统一用同一个普通 DNN 训练测试。

## 二、同预算下的结果

| 方法 | 说明 | 测试 R² |
|---|---|---|
| DGatingDNN ⭐ | 本文方法：可微稀疏门控（NeurIPS 2025） | **0.9703** |
| STG | 随机门控，高斯松弛逼近 L0（ICML 2020） | **0.9663** |
| LassoNet | 神经网络旁挂线性通路的特征级稀疏（JMLR 2021） | **0.9466** |
| Lasso | 线性稀疏（1996） | **0.9463** |
| XGBoost | 树模型特征重要性（2016） | **0.9387** |
| Pearson | 相关系数排序（只看两两关系） | **0.9214** |

> 同预算下最好的是 **DGatingDNN**（R² = 0.9703）。

### 各方法在这个预算下选中的字段

只看 R² 看不出方法差在哪。同样给几个字段，谁挑的是负荷、谁挑的是备用，这才是实质差别，也是判断结果合不合物理常识的依据。

| 方法 | 选中的字段（按各自的排序） |
|---|---|
| **DGatingDNN** | 区域3燃气出力、区域1风电出力、系统网损、光伏出力、风电出力、区域2燃气出力、区域2燃煤出力、区域1燃气出力、水电出力、核电出力、区域1水电出力 |
| STG | 区域3燃气出力、区域3光伏出力、系统网损、区域2燃气出力、区域1风电出力、区域3风电出力、核电出力、区域1水电出力、区域1核电出力、区域1燃气出力、水电出力 |
| LassoNet | 风电出力、系统网损、区域3光伏出力、区域1风电出力、区域1核电出力、区域1水电出力、区域1燃气出力、水电出力、核电出力、区域2燃气出力、区域3水电出力 |
| XGBoost | 区域1光伏出力、区域1风电出力、区域3光伏出力、光伏出力、系统网损、区域1发电总量、风电出力、区域3燃气出力、燃气出力、核电出力、区域1燃气出力 |
| Lasso | 区域1风电出力、区域3光伏出力、风电出力、核电出力、区域2燃气出力、区域2燃煤出力、区域1核电出力、区域1水电出力、区域1燃气出力、系统网损、区域3燃煤出力 |
| Pearson | 风电出力、区域3风电出力、区域1风电出力、系统网损、区域3光伏出力、光伏出力、区域1光伏出力、区域2光伏出力、区域2燃气出力、区域2燃煤出力、调频上备用 |

## 三、不同字段数下的完整曲线

只比一个预算容易碰巧，下表给出各方法在多个字段数下的表现（对应图 `fig_topk_curve.png`）：

| 方法 | k=1 | k=3 | k=5 | k=11 | k=16 | k=23 | k=43 |
|---|---|---|---|---|---|---|---|
| DGatingDNN | 0.0235 | 0.8010 | 0.8994 | 0.9703 | 0.9734 | 0.9753 | 0.9738 |
| STG | 0.0235 | 0.8532 | 0.9123 | 0.9663 | 0.9733 | 0.9738 | 0.9758 |
| LassoNet | 0.5078 | 0.8370 | 0.8823 | 0.9466 | 0.9567 | 0.9730 | 0.9758 |
| Lasso | 0.4376 | 0.7249 | 0.8544 | 0.9463 | 0.9693 | 0.9693 | 0.9759 |
| XGBoost | 0.3488 | 0.7000 | 0.8606 | 0.9387 | 0.9738 | 0.9748 | 0.9750 |
| Pearson | 0.5078 | 0.5393 | 0.8734 | 0.9214 | 0.9490 | 0.9749 | 0.9749 |

## 四、各方法的筛选过程

### DGatingDNN

- 门控模型自身的测试 R²：0.9642
- 活跃阈值 0.01，最终活跃字段 11 个
- 过程图 `DGatingDNN/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `DGatingDNN/gate_history.csv`

### STG

- 门控模型自身的测试 R²：0.9714
- 活跃阈值 0.0，最终活跃字段 14 个
- 过程图 `STG/fig_process.png`：每条曲线是一个字段的门控值随训练轮次的变化，左边线性刻度看保留字段的分化，右边对数刻度看被淘汰字段掉到多低
- 逐轮门控值记录在 `STG/gate_history.csv`

### LassoNet

- 它的筛选过程是一条 λ 路径：正则强度从小到大变化，字段被逐个剔除
- 路径共 436 个点，字段数从 0–43 变化
- 记录在 `LassoNet/lambda_path.csv`

### XGBoost / Lasso / Pearson

这三个不存在「逐轮收缩」的过程——它们一次拟合就给出全部字段的重要性排序，选多少个完全由外部指定的 k 决定。各字段得分见各自目录下的 `ranking.csv`。

## 五、各方法选中的字段重合情况

| 方法 | 与本文方法重合 | 重合率 |
|---|---|---|
| STG | 8 / 11 | 73% |
| LassoNet | 8 / 11 | 73% |
| XGBoost | 7 / 11 | 64% |
| Lasso | 8 / 11 | 73% |
| Pearson | 6 / 11 | 55% |

详见图 `fig_selection_matrix.png`。

## 六、本文方法选出的字段

| 排名 | 字段 | 业务含义 |
|---|---|---|
| 1 | `area_3_fuel_natural_gas_mw` | 区域3燃气出力 |
| 2 | `area_1_fuel_wind_mw` | 区域1风电出力 |
| 3 | `system_losses_mw` | 系统网损 |
| 4 | `gen_fuel_solar_mw` | 光伏出力 |
| 5 | `gen_fuel_wind_mw` | 风电出力 |
| 6 | `area_2_fuel_natural_gas_mw` | 区域2燃气出力 |
| 7 | `area_2_fuel_coal_mw` | 区域2燃煤出力 |
| 8 | `area_1_fuel_natural_gas_mw` | 区域1燃气出力 |
| 9 | `gen_fuel_hydro_mw` | 水电出力 |
| 10 | `gen_fuel_nuclear_mw` | 核电出力 |
| 11 | `area_1_fuel_hydro_mw` | 区域1水电出力 |

## 七、本目录下的图

- `fig_topk_curve.png`　各方法在不同字段数下的表现（信息量最大）
- `fig_comparison.png`　统一预算下的横向对比
- `fig_selection_matrix.png`　各方法选中了哪些字段
- `<方法>/fig_process.png`　该方法的筛选过程（仅门控类方法有）
