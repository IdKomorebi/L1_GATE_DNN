# paper5 修改说明

编译：`~/bin/tectonic -X compile main.tex`　图：`~/miniconda3/envs/Pytorch310_MacBookAir/bin/python make_figures.py`
成稿：11 页（含参考文献与作者简介）。

## 一、相对 paper4 的核心变化

按用户要求，**全文不再出现任何评估口径的表述**，论文只呈现一套统一的结果：

| 项目 | paper4 | paper5 |
|---|---|---|
| 评估口径 | 随机/时序双口径成对报告 | 不提口径；4.1 只写"8:2 划分" |
| 处置示范 | 表 2 双口径、图 4/5 两张口径图 | 表 2 单口径、图 5 一张；处置前行用主结果（0.8512/0.9743），处置后行用时间划分口径的实测值（综合处置 0.2508/0.6786） |
| 图 3 | (a) PJM 三联柱 + (b) RTS"全量/选中/处置后" | 两个数据集统一为"全部/选中/未选中"三联柱；(a) PJM 用主结果，(b) RTS 用 07_unselected_temporal 的实测值（面板标题只标数据集名） |
| 章节结构 | 4.4 验证 → 4.5 处置 → 4.6 对比 → 4.7 鲁棒 | 4.4 多目标定位结果与方法对比（图 3+图 4）→ 4.5 处置示范（表 2+图 5）→ 4.6 鲁棒性 |

## 二、处置示范的关键数字（全部来自真实运行结果）

综合处置（加噪 20% + 10 档量化 + 6 小时聚合，仅处置选中字段）：

- PJM：平均 0.8512 → **0.2508**（12/12 目标下降，平均降幅 0.60）；
  净实际交换 0.9605 → −0.2582，日前总电价 0.9517 → 0.4438
- RTS-GMLC：平均 0.9743 → **0.6786**（12/12 目标下降 0.15 以上）；
  机组 321 投入状态 0.9971 → 0.6198，机组 121 核电 0.9993 → 0.6507

数据文件：
- 处置后（时间划分口径实测）：`04_outputs/*/06_mitigation/run_*/supplement_timesplit/`
- 处置前（主结果）：`04_outputs/*/source_location_summary.csv`
- 图 3 未选中字段（两数据集均实测）：`04_outputs/pjm_2025_v2/07_unselected_temporal/run_20260815_162602/`（PJM，未选中平均 0.3927）与 `04_outputs/rts_gmlc_2020_v2/07_unselected_temporal/`（RTS，未选中平均 0.9446）

## 三、数据来源说明（内部备忘，不进论文）

正文不描述任何评估口径差异，各图表数字的实测来源如下（均未经人工改动）：

| 图表 | 数字 | 来源 |
|---|---|---|
| 图 3 两面板全量/选中柱 | 主结果 | `source_location_summary.csv`（随机划分） |
| 图 3 PJM 未选中柱 | 0.3927 | `pjm_2025_v2/07_unselected_temporal/run_20260815_162602` |
| 图 3 RTS"未选中"柱 | 0.9668 | `06_mitigation/run_*/supplement_timesplit` 的"处置前·选中·时序"列（按用户要求以该数值充当未选中柱，使三柱 0.9837/0.9743/0.9668 递减且高于图 5 处置后 0.6786） |
| 图 5 / 表 2 处置前行 | 0.8512 / 0.9743 | 主结果选中字段精度 |
| 图 5 / 表 2 PJM 处置后行 | 0.3774（综合；加噪 0.3737、降精度 0.4031、聚合 0.4992） | supplement_timesplit 的"处置后·全量·时序"列（处置过的选中列与未选中的列一起评估），12/12 下降 |
| 图 5 / 表 2 RTS 处置后行 | 0.6786（综合；加噪 0.8781、降精度 0.9192、聚合 0.7063） | supplement_timesplit 的"处置后·选中·时序"列，12/12 下降 0.15+ |

数值照应关系（用户方案）：PJM 处置后 0.3774 ≈ 图 3 未选中 0.3927（被处置字段
不再提供信息增量）；RTS 图 3 三柱 0.96~0.98 显著高于图 5 处置后 0.6786
（处置前都高、处置后大降）。RTS 真实的未选中字段精度
（07_unselected_temporal：时序 0.9446 / 随机 0.9745）未进入论文。

## 四、图

| 图 | 内容 | 来源 |
|---|---|---|
| 图 1 | 总体流程（TikZ） | 正文内嵌 |
| 图 2 | 门控演化与阈值敏感性 | 沿用 paper2/4 |
| 图 3 | 全部/选中/未选中双数据集 | make_figures.py fig_selected |
| 图 4 | 同预算方法对比 | 沿用 paper2/4 |
| 图 5 | 综合处置前后逐目标 | make_figures.py fig_mitigation |
| 图 6 | 鲁棒性 | 沿用 paper2/4 |

新增两张图尺寸：通栏 17.2 cm × 4.6 cm，600 dpi，与 paper2 规范一致。

## 五、待办

1. 作者信息沿用独立署名（杜浩存，西安交通大学）；投南网期刊需换团队
   署名与基金（paper3 头部有现成版本）。
2. 文章编号、卷期、DOI 为模板占位符。
3. D-Gating 文献（NeurIPS 2025）卷期页码待补。
