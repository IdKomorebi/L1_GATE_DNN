# paper7 修改说明

编译：`~/bin/tectonic -X compile main.tex`
数据：`~/miniconda3/envs/Pytorch310_MacBookAir/bin/python make_figure_data.py`（重新生成 figure_data.json）
画图：`~/miniconda3/envs/Pytorch310_MacBookAir/bin/python make_figures.py`（只读 figure_data.json）
成稿：11 页，6 图（11 子图文件）3 表，参考文献 33 条按引用顺序排列。
图与数值与 paper6 完全一致（figure_data.json 未变）。

## 一、13 条意见的落实

| # | 意见 | 落实 |
|---|---|---|
| 1 | 题目 | 《面向电力数据发布前审查的隐性推断源定位方法》，"电力数据"整体出现；处置只是闭环末端的示范验证，不进标题 |
| 2 | 摘要逻辑主线 | 摘要第二句起即写两类还原关系（已定义规则公式/物理规律 + 线性/乘积可拟合的近似关系 → 可显式写出逐条剥离；剥离后剩余为隐性推断），方法句按"先剔除已知公式，再迭代发现近似关系，再门控定位"展开；结尾保留处置示范与"定位结果可直接作为处置操作对象"的落点 |
| 3 | 引言主线 | 第一段开篇即立两条纽带："可显式写出的公式关系"与"没有公式却稳定存在的统计依赖"，并点明分别对应公式关系与隐性推断、是全文主线；共享/推断威胁文献段顺势收束到隐性推断源定义；审查难点段区分"公式可查 / 隐性推断无从下手"；"为什么定位"段保留 |
| 4 | 参考文献 | 33 条按正文首次引用顺序重排；新增 GB/T 43697—2024《数据安全技术 数据分类分级规则》；删除不再引用的 Goodfellow 专著 |
| 5/9 | 方法归属 | 3.3 节改写为"本文构建作用于输入字段的稀疏门控网络作为定位模型……门控值的参数化借鉴文献[31]提出的结构稀疏化思路"；摘要写"门控值取多因子乘积形式的分解式稀疏门控深度网络"，不再以方法名为主语 |
| 6 | 本文工作 | 由 4 点并为 3 点：问题形式化与两类关系划分 / 规则公式与近似关系剥离方法 / 定位模型与处置示范闭环；不再出现审稿意见原句 |
| 7 | 第 2 章 | 2.2 重写为"两类还原关系"：第一类含两种来源（已定义的规则公式与物理规律；数据中自行发现、线性或对数线性可高精度拟合的近似关系），第二类为多因素耦合的隐性推断；隐性推断源定义并入段末（"对隐性推断起主要作用的字段集合……构成处置的直接依据"），删去原 2.3 的 A* 形式化定义（暴露"只找最紧凑、不保证其余无用"的逻辑缺口） |
| 8 | 第 3 章剥离 | 3.2+3.3 合并为"规则公式与近似关系的剥离"：第一步剔除已知公式（覆盖全部汇总层级、集合表达式），第二步迭代发现近似关系（线性/取对数线性拟合、R²>0.95 判定、每轮剥一个贡献最大字段、重新拟合直至无关系）；反向消元与每轮一个字段的设计理由保留；流程图改为两步衔接的单阶段表述 |
| 10 | 3.4 措辞 | 改为"处置只作用于 A* 中的字段，其余字段照常发布"；4.5 开头同步 |
| 11 | 4.1 客观化 | 删去"分别承担不同的论证角色""公开与敏感边界由实际发布惯例决定，而非人为指定密级"等主观句；改为客观描述：PJM 69 字段、目标为运行状态量与市场量共 12 个（点名交换功率/网损/发电/负荷/电价分量/备用），每次遮蔽其一；RTS 候选 58 个汇总量、目标 12 个设备级量 |
| 12 | 4.2 公式表 | 新增表 1：PJM 四条代表性近似关系（两类阻塞价、净计划交换+常数、功率平衡统计形式）+ 相对残差 + 末行省略号；判定依据按 R²>0.95 表述（3.2 与 4.2 一致）；未列恒等式与重复字段类 |
| 13 | 4.3 | 开头明确"将该字段遮蔽为受保护目标，以剥离后的 48 个候选字段为输入"；删除"边际相关性排第 40、秩相关 0.057"段落（引言相关工作中保留一句定性论断） |

## 二、其余同步修改

- 结论并为 3 条，与贡献点一一对应；鲁棒性结论并入第 3 条。
- 关键词调整顺序：电力数据安全；数据发布审查；隐性推断源；稀疏门控；深度神经网络。
- 中英文摘要、图 1 流程图（两步剥离合并为一阶段）、表 2 表头（"已知公式/近似关系"）同步更新。
- 引用顺序核对：正文首次出现顺序 = bib.tex 条目顺序（rtsgmlc 在 kingma 之前，已核对）。

## 三、待办

1. 作者信息沿用独立署名；投南网期刊需换团队署名与基金。
2. 文章编号、卷期、DOI 为模板占位符。
3. D-Gating 文献（NeurIPS 2025）卷期页码待正式出版信息。
