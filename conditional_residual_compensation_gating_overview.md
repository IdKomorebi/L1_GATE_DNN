# 条件化残差补偿门控方法：整体思路说明

## 0. 方法定位

本方法不是普通特征选择，也不是从全部字段中寻找一个最小输入子集。普通稀疏门控方法通常可以做到：给每个输入字段设置门控，通过稀疏约束保留少量字段，从而得到一条主要推断路径。

但是普通门控有一个关键缺点：**通常只能找到一条主路径**。

假设全字段集合为：

$$
U=\{x_1,x_2,\dots,x_p\}
$$

目标字段为：

$$
y
$$

普通门控可能找到一条主推断路径：

$$
P=\{x_1,x_2,\dots,x_{10}\}
$$

这说明 $P$ 能够较好推断 $y$。但是这并不能说明其他字段没有风险。路径外字段可能无法单独恢复 $y$，但它们可能在主路径缺少某个字段时补偿这个缺口。

因此，本方法的目标不是重新找一个全局最小子集，而是进一步分析：

> 主路径 $P$ 中的每个字段 $x_i$，是否可以被路径外字段 $R=U\setminus P$ 替代？如果可以，替代字段组是什么？

---

## 1. 从“找重要字段”转向“找替代关系”

假设阶段1已经得到主路径：

$$
P=\{x_1,x_2,\dots,x_{10}\}
$$

剩余字段为：

$$
R=U\setminus P
$$

对于主路径中的某个字段 $x_i$，定义：

$$
C_i=P\setminus\{x_i\}
$$

其中 $C_i$ 表示删除 $x_i$ 后，主路径中剩余的字段。我们把 $C_i$ 看作固定上下文。

我们真正关心的是是否存在：

$$
Q_i\subseteq R
$$

使得：

$$
C_i\cup Q_i
$$

仍然能够较好预测 $y$。

如果存在这样的 $Q_i$，说明 $x_i$ 不是不可替代的，它可以被路径外字段组 $Q_i$ 补偿。

因此，本方法的核心对象是：

$$
\boxed{
\text{固定上下文 } C_i \text{ 下，被删除字段 } x_i \text{ 的替代字段组 } Q_i
}
$$

这与普通意义上的全局特征子集选择不同。

---

## 2. 为什么要先验证可替代性

在对 $x_i$ 做复杂替代分析之前，需要先做三个简单验证。

### 2.1 验证 $C_i$ 本身是否达标

如果：

$$
C_i=P\setminus\{x_i\}
$$

本身就能预测 $y$，说明 $x_i$ 在主路径中其实是冗余的，阶段1得到的主路径还没有剪枝干净。

此时应当回到阶段1重新压缩主路径，而不是继续为 $x_i$ 寻找替代字段。

### 2.2 验证 $C_i\cup R$ 是否达标

如果：

$$
C_i\cup R
$$

都不能达标，说明即使把路径外所有字段都加入，也无法补回 $x_i$ 的作用。

此时 $x_i$ 是当前主路径下的不可替代字段，不需要继续找替代组。

### 2.3 验证 $R$ 是否单独达标

如果：

$$
R
$$

单独达标，说明存在完全绕开主路径 $P$ 的另一条独立路径。

如果 $R$ 单独不达标，但：

$$
C_i\cup R
$$

达标，则说明路径外字段不能单独成路，只能在保留 $C_i$ 的条件下补偿 $x_i$。

这正是本方法要分析的“条件替代”场景。

---

## 3. 为什么不用普通联合门控直接找 $Q_i$

一种直接想法是训练：

$$
\hat y=f_\theta([x_{C_i};g\odot x_R])
$$

然后根据 $R$ 中字段的门控值判断哪些字段重要。

但是这个结构有问题。

因为它直接预测的是 $y$。这样 $R$ 中只要有字段对 $y$ 有帮助，就可能被选中。

可是我们真正想找的是：

> 哪些字段能补偿删除 $x_i$ 后留下的信息缺口？

如果某个路径外字段只是和 $C_i$ 中已有字段冗余，它也可能帮助预测 $y$，但它并不是 $x_i$ 的替代字段。

因此，直接联合预测 $y$ 容易产生假阳性。

---

## 4. 为什么改成残差补偿

为了解决上述问题，先用 $C_i$ 单独预测 $y$：

$$
\hat y_C=f_C(x_{C_i})
$$

然后计算残差：

$$
e_i=y-\hat y_C
$$

这个残差表示：

$$
\boxed{
C_i \text{ 已经解释不了的那部分目标信息}
}
$$

由于 $C_i$ 是主路径删除 $x_i$ 后剩下的字段，因此这个残差可以理解为：

$$
\boxed{
删除 x_i 后留下的信息缺口}
}
$$

接下来，不再让 $R$ 直接预测 $y$，而是让 $R$ 在 $C_i$ 的条件下预测残差：

$$
\hat e_i=h_\theta(x_{C_i},g\odot x_R)
$$

最终预测可以写成：

$$
\hat y=f_C(x_{C_i})+\hat e_i
$$

这样，候选字段 $R$ 的任务就变成了：

> 不是泛泛帮助预测 $y$，而是专门补偿 $C_i$ 解释不了的残差。

这更符合“替代 $x_i$”的定义。

---

## 5. 为什么残差补偿网络还要输入 $C_i$

残差补偿网络不能只写成：

$$
\hat e_i=h_\theta(g\odot x_R)
$$

更合理的结构是：

$$
\hat e_i=h_\theta(x_{C_i},g\odot x_R)
$$

原因是：删除 $x_i$ 后的缺口可能和上下文 $C_i$ 有交互关系。

例如：

$$
y=x_i\cdot x_2+x_3
$$

如果删除 $x_i$，缺失的部分不是单独的 $x_i$，而是：

$$
x_i\cdot x_2
$$

如果路径外字段 $r_7$ 可以替代 $x_i$，那么它必须和 $x_2$ 一起作用：

$$
r_7\cdot x_2
$$

所以补偿网络必须看到 $C_i$，才能学习：

> 在当前上下文下，路径外字段如何补偿缺口。

这就是“条件化残差补偿”。

---

## 6. 为什么显影阶段要用 dropout

显影阶段的目的不是立刻选出最小替代组，而是先找出哪些路径外字段具有补偿潜力。

如果直接用强 L1 稀疏门控，模型可能只保留一个最容易的替代字段，把其他可替代字段压掉。

但本方法想找的是“可能替代 $x_i$ 的候选字段”，不能一开始就把冗余替代字段杀掉。

所以显影阶段使用较弱的稀疏项，同时对候选字段门控施加 dropout：

$$
d\odot g\odot x_R
$$

其中 $d$ 是随机失活 mask。

这样训练时某些候选字段会随机不可用，模型不能把补偿能力完全压在单一字段上，多个具有补偿能力的字段会被迫显影出来。

显影阶段输出的是活跃候选集：

$$
A\subseteq R
$$

它不是最终替代组，而是后续锚定多头模型的候选锚字段来源。

---

## 7. 为什么用锚定多头，而不是普通多头

如果直接设置多个门控头：

$$
g^{(1)},g^{(2)},\dots,g^{(K)}
$$

让每个头都去找一个替代组，那么多个头很容易学到同一个最容易的组合。

为了解决这个问题，本方法不给所有头完全相同的问题，而是让每个头围绕不同的锚字段寻找替代组。

假设显影阶段得到活跃集：

$$
A=\{a_1,a_2,\dots,a_K\}
$$

那么设置 $K$ 个头。第 $k$ 个头强制包含锚字段 $a_k$：

$$
z_{k,a_k}=1
$$

其他字段再通过门控学习：

$$
z_{k,j},\quad j\neq a_k
$$

第 $k$ 个头输出：

$$
Q_k=\{a_k\}\cup\{r_j:z_{k,j}=1\}
$$

这样每个头都从不同锚字段出发，天然避免所有头完全坍缩到同一个解。

同时，头之间不加互斥约束，所以允许部分重叠。如果某个字段是多个替代组共享的替代枢纽，它可以自然出现在多个头中。

---

## 8. 为什么不设置可学习“软基座”

之前考虑过基座—增量结构，例如：

$$
g_i^k=b_i+(1-b_i)u_i^k
$$

但这个结构会出现 $b/u$ 代偿问题。

模型只关心最终输入门控 $g_i^k$，不关心字段到底由 $b_i$ 进入，还是由 $u_i^k$ 进入。因此容易出现：

$$
b_i \text{ 高，}u_i^k \text{ 也高}
$$

解释不清。

现在的方案不再学习一个“软基座”。固定上下文就是：

$$
C_i=P\setminus\{x_i\}
$$

它是外部确定的，不参与学习。

而替代枢纽、共享字段等结构，不由模型内部参数表示，而是在得到多个替代组后做事后统计。

例如，如果：

$$
Q_1=\{r_1,r_3\}
$$

$$
Q_2=\{r_1,r_7\}
$$

$$
Q_3=\{r_1,r_{12}\}
$$

那么 $r_1$ 出现在多个替代组里，说明它是替代枢纽。

这种读出方式比学习一个软基座更稳定。

---

## 9. 最终认证为什么必须回到原始任务

残差补偿只是用来发现候选替代组。

最终判断某个替代组 $Q_k$ 是否有效，不能只看它能不能拟合残差，而必须回到原始任务：

$$
C_i\cup Q_k \rightarrow y
$$

如果：

$$
R^2(C_i\cup Q_k)\geq \tau
$$

才说明 $Q_k$ 是有效替代字段组。

然后还要做局部去冗余。如果：

$$
C_i\cup(Q_k\setminus\{r_j\})
$$

仍然达标，说明 $r_j$ 是冗余字段，应当删除。

最终保留的是去冗余后的替代组。

---

# 总体流程

## 阶段1：主路径识别

从全字段 $U$ 中通过稀疏门控或 L1DNN 找到一条去冗余主路径：

$$
P
$$

并验证它接近全字段模型性能。

输出：

$$
P,\quad R=U\setminus P
$$

---

## 阶段2：主路径字段可替代性验证

对每个：

$$
x_i\in P
$$

构造：

$$
C_i=P\setminus\{x_i\}
$$

验证三件事：

1. $C_i$ 是否达标；
2. $C_i\cup R$ 是否达标；
3. $R$ 是否单独达标。

根据结果把 $x_i$ 分为：

- 主路径内冗余字段；
- 不可替代字段；
- 可替代字段。

只有可替代字段进入后续阶段。

---

## 阶段3：生成条件化残差

对可替代字段 $x_i$，用 $C_i$ 训练基座模型：

$$
\hat y_C=f_C(x_{C_i})
$$

用 out-of-fold 方式生成残差：

$$
e_i=y-\hat y_C
$$

并标准化残差：

$$
\tilde e_i=\frac{e_i}{\operatorname{std}(e_i)}
$$

输出 $x_i$ 的缺口能量：

$$
Gap_i=\frac{\operatorname{Var}(e_i)}{\operatorname{Var}(y)}
$$

---

## 阶段4：条件化残差显影

训练残差补偿网络：

$$
\hat{\tilde e}_i=h_\theta(x_{C_i},d\odot g\odot x_R)
$$

其中 $d$ 是 dropout mask，$g$ 是候选字段门控。

显影阶段的目标是找出有补偿潜力的候选字段，而不是直接输出替代组。

通过门控值和遮蔽敏感度得到活跃候选集：

$$
A\subseteq R
$$

---

## 阶段5：锚定多头残差补偿

对活跃集 $A$ 中每个字段设置一个锚定头。

第 $k$ 个头强制包含锚字段 $a_k$：

$$
z_{k,a_k}=1
$$

训练多头残差补偿模型：

$$
\hat{\tilde e}_i^{(k)}
=
h_\theta(x_{C_i},z^{(k)}\odot x_R)
$$

每个头输出一个候选替代组：

$$
Q_k
$$

---

## 阶段6：替代组认证与结果读出

对每个候选替代组 $Q_k$，回到原始目标任务验证：

$$
C_i\cup Q_k\rightarrow y
$$

如果达标，则保留。

然后做局部消融，得到去冗余替代组。

最后输出：

- $x_i$ 是否可替代；
- 有效替代组；
- 最小替代组规模；
- 替代枢纽字段；
- 替代类型；
- 缺口能量；
- 最佳替代组性能。

---

# 一句话总结

本方法的核心逻辑是：

$$
\boxed{
\text{先用门控找到一条主路径；}
\text{再逐个删除主路径字段；}
\text{对可替代字段，用固定上下文产生残差；}
\text{让路径外字段在上下文条件下补偿残差；}
\text{通过 dropout 显影候选字段；}
\text{再用锚定多头一次性形成多个主导替代组；}
\text{最后回到原始任务认证。}
}
$$

它不是普通特征选择，因为普通特征选择只回答：

$$
\text{哪些字段重要？}
$$

而本方法回答的是：

$$
\boxed{
\text{主路径中的某个重要字段被删除后，路径外哪些字段组可以补偿它？}
}
$$

这就是整个方法的核心创新点。
